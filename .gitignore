# PDM / local interpreter
.local-pdm/
.venv/
.pdm-python

# Python caches
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/

# VUnit build/cache output (regenerated by scripts/run_tb_vunit_axi.py)
vunit_out/

# GHDL toolchain + xpm_vhdl checkout and build output (scripts/run_tb_ghdl.sh)
.ghdl-deps/
.ghdl-build/

# Note: gen/ is intentionally tracked (generated register artifacts are committed),
# and pdm.lock is committed for reproducible installs.
