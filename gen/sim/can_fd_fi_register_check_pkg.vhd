-- -----------------------------------------------------------------------------
-- This file is automatically generated by hdl-registers version 8.1.0.
-- Code generator VhdlSimulationCheckPackageGenerator version 1.2.1.
-- -----------------------------------------------------------------------------

library ieee;
use ieee.fixed_pkg.all;
use ieee.numeric_std.all;
use ieee.std_logic_1164.all;

library vunit_lib;
use vunit_lib.bus_master_pkg.bus_master_t;
use vunit_lib.check_pkg.all;
use vunit_lib.checker_pkg.all;
use vunit_lib.com_types_pkg.network_t;
use vunit_lib.string_ops.hex_image;

library register_file;
use register_file.register_file_pkg.register_t;
use register_file.register_operations_pkg.register_bus_master;

use work.can_fd_fi_register_read_write_pkg.all;
use work.can_fd_fi_register_record_pkg.all;
use work.can_fd_fi_regs_pkg.all;


package can_fd_fi_register_check_pkg is

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'CORE_INFO' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_CORE_INFO_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'CORE_INFO' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_CORE_INFO_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_CORE_INFO_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'info_t' field in the 'CORE_INFO' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_CORE_INFO_info_t_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_CORE_INFO_info_t_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'CORE_VERSION' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_CORE_VERSION_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'CORE_VERSION' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_CORE_VERSION_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_CORE_VERSION_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'magic_valid' field in the 'CORE_VERSION' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_CORE_VERSION_magic_valid_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_CORE_VERSION_magic_valid_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'patch' field in the 'CORE_VERSION' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_CORE_VERSION_patch_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_CORE_VERSION_patch_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'minor' field in the 'CORE_VERSION' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_CORE_VERSION_minor_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_CORE_VERSION_minor_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'major' field in the 'CORE_VERSION' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_CORE_VERSION_major_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_CORE_VERSION_major_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'STATUS_FLAGS' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_STATUS_FLAGS_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'STATUS_FLAGS' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_STATUS_FLAGS_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_STATUS_FLAGS_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'tu_ready_config' field in the 'STATUS_FLAGS' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_STATUS_FLAGS_tu_ready_config_equal(
    signal net : inout network_t;
    expected : in std_ulogic;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'inj_err_miss' field in the 'STATUS_FLAGS' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_STATUS_FLAGS_inj_err_miss_equal(
    signal net : inout network_t;
    expected : in std_ulogic;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'inj_err_dom' field in the 'STATUS_FLAGS' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_STATUS_FLAGS_inj_err_dom_equal(
    signal net : inout network_t;
    expected : in std_ulogic;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'inj_err_dlc' field in the 'STATUS_FLAGS' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_STATUS_FLAGS_inj_err_dlc_equal(
    signal net : inout network_t;
    expected : in std_ulogic;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'inj_err_fsm' field in the 'STATUS_FLAGS' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_STATUS_FLAGS_inj_err_fsm_equal(
    signal net : inout network_t;
    expected : in std_ulogic;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'GLOBAL_CONFIG' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_GLOBAL_CONFIG_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'GLOBAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_GLOBAL_CONFIG_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_GLOBAL_CONFIG_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'cfg_en' field in the 'GLOBAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_GLOBAL_CONFIG_cfg_en_equal(
    signal net : inout network_t;
    expected : in std_ulogic;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'cfg_brp_nomi' field in the 'GLOBAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_GLOBAL_CONFIG_cfg_brp_nomi_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_GLOBAL_CONFIG_cfg_brp_nomi_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'cfg_brp_data' field in the 'GLOBAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_GLOBAL_CONFIG_cfg_brp_data_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_GLOBAL_CONFIG_cfg_brp_data_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'cfg_td_en' field in the 'GLOBAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_GLOBAL_CONFIG_cfg_td_en_equal(
    signal net : inout network_t;
    expected : in std_ulogic;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'cfg_ssp' field in the 'GLOBAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_GLOBAL_CONFIG_cfg_ssp_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_GLOBAL_CONFIG_cfg_ssp_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'NOMINAL_CONFIG' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_NOMINAL_CONFIG_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'NOMINAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_NOMINAL_CONFIG_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_NOMINAL_CONFIG_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'cfg_ph1_len_nomi' field in the 'NOMINAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_NOMINAL_CONFIG_cfg_ph1_len_nomi_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_NOMINAL_CONFIG_cfg_ph1_len_nomi_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'cfg_ph2_len_nomi' field in the 'NOMINAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_NOMINAL_CONFIG_cfg_ph2_len_nomi_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_NOMINAL_CONFIG_cfg_ph2_len_nomi_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'cfg_sjw_nomi' field in the 'NOMINAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_NOMINAL_CONFIG_cfg_sjw_nomi_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_NOMINAL_CONFIG_cfg_sjw_nomi_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'DATA_CONFIG' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_DATA_CONFIG_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'DATA_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_DATA_CONFIG_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_DATA_CONFIG_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'cfg_ph1_len_data' field in the 'DATA_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_DATA_CONFIG_cfg_ph1_len_data_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_DATA_CONFIG_cfg_ph1_len_data_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'cfg_ph2_len_data' field in the 'DATA_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_DATA_CONFIG_cfg_ph2_len_data_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_DATA_CONFIG_cfg_ph2_len_data_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'cfg_sjw_data' field in the 'DATA_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_DATA_CONFIG_cfg_sjw_data_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_DATA_CONFIG_cfg_sjw_data_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'FI_CONFIG' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_FI_CONFIG_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'FI_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_FI_CONFIG_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_FI_CONFIG_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'cfg_psp_nomi' field in the 'FI_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_FI_CONFIG_cfg_psp_nomi_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_FI_CONFIG_cfg_psp_nomi_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'cfg_psp_data' field in the 'FI_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_FI_CONFIG_cfg_psp_data_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_FI_CONFIG_cfg_psp_data_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'cfg_fip_nomi' field in the 'FI_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_FI_CONFIG_cfg_fip_nomi_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_FI_CONFIG_cfg_fip_nomi_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'cfg_fip_data' field in the 'FI_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_FI_CONFIG_cfg_fip_data_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_FI_CONFIG_cfg_fip_data_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'IRQ_RST_REG' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_IRQ_RST_REG_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'IRQ_RST_REG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_IRQ_RST_REG_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_IRQ_RST_REG_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'irq_rst' field in the 'IRQ_RST_REG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_IRQ_RST_REG_irq_rst_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_IRQ_RST_REG_irq_rst_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM0_MATCH' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM0_MATCH_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM0_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM0_MATCH_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM0_MATCH_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm0_match' field in the 'ID_MEM0_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM0_MATCH_idm0_match_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM0_MATCH_idm0_match_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM0_MASK' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM0_MASK_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM0_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM0_MASK_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM0_MASK_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm0_mask' field in the 'ID_MEM0_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM0_MASK_idm0_mask_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM0_MASK_idm0_mask_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM0_FIF' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM0_FIF_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM0_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM0_FIF_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM0_FIF_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm0_fif' field in the 'ID_MEM0_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM0_FIF_idm0_fif_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM0_FIF_idm0_fif_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM1_MATCH' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM1_MATCH_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM1_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM1_MATCH_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM1_MATCH_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm1_match' field in the 'ID_MEM1_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM1_MATCH_idm1_match_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM1_MATCH_idm1_match_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM1_MASK' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM1_MASK_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM1_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM1_MASK_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM1_MASK_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm1_mask' field in the 'ID_MEM1_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM1_MASK_idm1_mask_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM1_MASK_idm1_mask_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM1_FIF' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM1_FIF_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM1_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM1_FIF_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM1_FIF_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm1_fif' field in the 'ID_MEM1_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM1_FIF_idm1_fif_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM1_FIF_idm1_fif_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM2_MATCH' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM2_MATCH_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM2_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM2_MATCH_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM2_MATCH_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm2_match' field in the 'ID_MEM2_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM2_MATCH_idm2_match_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM2_MATCH_idm2_match_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM2_MASK' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM2_MASK_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM2_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM2_MASK_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM2_MASK_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm2_mask' field in the 'ID_MEM2_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM2_MASK_idm2_mask_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM2_MASK_idm2_mask_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM2_FIF' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM2_FIF_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM2_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM2_FIF_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM2_FIF_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm2_fif' field in the 'ID_MEM2_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM2_FIF_idm2_fif_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM2_FIF_idm2_fif_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM3_MATCH' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM3_MATCH_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM3_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM3_MATCH_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM3_MATCH_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm3_match' field in the 'ID_MEM3_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM3_MATCH_idm3_match_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM3_MATCH_idm3_match_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM3_MASK' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM3_MASK_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM3_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM3_MASK_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM3_MASK_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm3_mask' field in the 'ID_MEM3_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM3_MASK_idm3_mask_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM3_MASK_idm3_mask_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM3_FIF' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM3_FIF_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM3_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM3_FIF_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM3_FIF_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm3_fif' field in the 'ID_MEM3_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM3_FIF_idm3_fif_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM3_FIF_idm3_fif_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM4_MATCH' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM4_MATCH_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM4_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM4_MATCH_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM4_MATCH_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm4_match' field in the 'ID_MEM4_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM4_MATCH_idm4_match_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM4_MATCH_idm4_match_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM4_MASK' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM4_MASK_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM4_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM4_MASK_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM4_MASK_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm4_mask' field in the 'ID_MEM4_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM4_MASK_idm4_mask_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM4_MASK_idm4_mask_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM4_FIF' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM4_FIF_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM4_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM4_FIF_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM4_FIF_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm4_fif' field in the 'ID_MEM4_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM4_FIF_idm4_fif_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM4_FIF_idm4_fif_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM5_MATCH' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM5_MATCH_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM5_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM5_MATCH_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM5_MATCH_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm5_match' field in the 'ID_MEM5_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM5_MATCH_idm5_match_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM5_MATCH_idm5_match_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM5_MASK' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM5_MASK_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM5_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM5_MASK_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM5_MASK_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm5_mask' field in the 'ID_MEM5_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM5_MASK_idm5_mask_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM5_MASK_idm5_mask_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM5_FIF' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM5_FIF_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM5_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM5_FIF_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM5_FIF_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm5_fif' field in the 'ID_MEM5_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM5_FIF_idm5_fif_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM5_FIF_idm5_fif_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM6_MATCH' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM6_MATCH_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM6_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM6_MATCH_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM6_MATCH_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm6_match' field in the 'ID_MEM6_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM6_MATCH_idm6_match_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM6_MATCH_idm6_match_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM6_MASK' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM6_MASK_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM6_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM6_MASK_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM6_MASK_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm6_mask' field in the 'ID_MEM6_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM6_MASK_idm6_mask_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM6_MASK_idm6_mask_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM6_FIF' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM6_FIF_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM6_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM6_FIF_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM6_FIF_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm6_fif' field in the 'ID_MEM6_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM6_FIF_idm6_fif_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM6_FIF_idm6_fif_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM7_MATCH' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM7_MATCH_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM7_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM7_MATCH_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM7_MATCH_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm7_match' field in the 'ID_MEM7_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM7_MATCH_idm7_match_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM7_MATCH_idm7_match_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM7_MASK' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM7_MASK_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM7_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM7_MASK_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM7_MASK_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm7_mask' field in the 'ID_MEM7_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM7_MASK_idm7_mask_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM7_MASK_idm7_mask_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM7_FIF' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM7_FIF_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'ID_MEM7_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM7_FIF_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM7_FIF_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );

  -- Check that the current value of the 'idm7_fif' field in the 'ID_MEM7_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM7_FIF_idm7_fif_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM7_FIF_idm7_fif_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  );
  -- ---------------------------------------------------------------------------

end package;

package body can_fd_fi_register_check_pkg is

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'CORE_INFO' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_CORE_INFO_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'CORE_INFO' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_CORE_INFO(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'CORE_INFO' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_CORE_INFO_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_CORE_INFO_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'CORE_INFO' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_CORE_INFO_t;
  begin
    read_can_fd_fi_CORE_INFO(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'info_t' field in the 'CORE_INFO' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_CORE_INFO_info_t_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_CORE_INFO_info_t_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'info_t' field in the 'CORE_INFO' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_CORE_INFO_t := can_fd_fi_CORE_INFO_init;
    variable got : can_fd_fi_CORE_INFO_info_t_t := can_fd_fi_CORE_INFO_info_t_init;
  begin
    read_can_fd_fi_CORE_INFO(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.info_t;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'CORE_VERSION' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_CORE_VERSION_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'CORE_VERSION' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_CORE_VERSION(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'CORE_VERSION' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_CORE_VERSION_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_CORE_VERSION_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'CORE_VERSION' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_CORE_VERSION_t;
  begin
    read_can_fd_fi_CORE_VERSION(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'magic_valid' field in the 'CORE_VERSION' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_CORE_VERSION_magic_valid_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_CORE_VERSION_magic_valid_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'magic_valid' field in the 'CORE_VERSION' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_CORE_VERSION_t := can_fd_fi_CORE_VERSION_init;
    variable got : can_fd_fi_CORE_VERSION_magic_valid_t := can_fd_fi_CORE_VERSION_magic_valid_init;
  begin
    read_can_fd_fi_CORE_VERSION(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.magic_valid;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'patch' field in the 'CORE_VERSION' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_CORE_VERSION_patch_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_CORE_VERSION_patch_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'patch' field in the 'CORE_VERSION' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_CORE_VERSION_t := can_fd_fi_CORE_VERSION_init;
    variable got : can_fd_fi_CORE_VERSION_patch_t := can_fd_fi_CORE_VERSION_patch_init;
  begin
    read_can_fd_fi_CORE_VERSION(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.patch;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'minor' field in the 'CORE_VERSION' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_CORE_VERSION_minor_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_CORE_VERSION_minor_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'minor' field in the 'CORE_VERSION' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_CORE_VERSION_t := can_fd_fi_CORE_VERSION_init;
    variable got : can_fd_fi_CORE_VERSION_minor_t := can_fd_fi_CORE_VERSION_minor_init;
  begin
    read_can_fd_fi_CORE_VERSION(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.minor;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'major' field in the 'CORE_VERSION' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_CORE_VERSION_major_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_CORE_VERSION_major_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'major' field in the 'CORE_VERSION' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_CORE_VERSION_t := can_fd_fi_CORE_VERSION_init;
    variable got : can_fd_fi_CORE_VERSION_major_t := can_fd_fi_CORE_VERSION_major_init;
  begin
    read_can_fd_fi_CORE_VERSION(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.major;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'STATUS_FLAGS' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_STATUS_FLAGS_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'STATUS_FLAGS' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_STATUS_FLAGS(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'STATUS_FLAGS' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_STATUS_FLAGS_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_STATUS_FLAGS_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'STATUS_FLAGS' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_STATUS_FLAGS_t;
  begin
    read_can_fd_fi_STATUS_FLAGS(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'tu_ready_config' field in the 'STATUS_FLAGS' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_STATUS_FLAGS_tu_ready_config_equal(
    signal net : inout network_t;
    expected : in std_ulogic;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'tu_ready_config' field in the 'STATUS_FLAGS' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_STATUS_FLAGS_t := can_fd_fi_STATUS_FLAGS_init;
    variable got : std_ulogic := can_fd_fi_STATUS_FLAGS_tu_ready_config_init;
  begin
    read_can_fd_fi_STATUS_FLAGS(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.tu_ready_config;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'inj_err_miss' field in the 'STATUS_FLAGS' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_STATUS_FLAGS_inj_err_miss_equal(
    signal net : inout network_t;
    expected : in std_ulogic;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'inj_err_miss' field in the 'STATUS_FLAGS' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_STATUS_FLAGS_t := can_fd_fi_STATUS_FLAGS_init;
    variable got : std_ulogic := can_fd_fi_STATUS_FLAGS_inj_err_miss_init;
  begin
    read_can_fd_fi_STATUS_FLAGS(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.inj_err_miss;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'inj_err_dom' field in the 'STATUS_FLAGS' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_STATUS_FLAGS_inj_err_dom_equal(
    signal net : inout network_t;
    expected : in std_ulogic;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'inj_err_dom' field in the 'STATUS_FLAGS' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_STATUS_FLAGS_t := can_fd_fi_STATUS_FLAGS_init;
    variable got : std_ulogic := can_fd_fi_STATUS_FLAGS_inj_err_dom_init;
  begin
    read_can_fd_fi_STATUS_FLAGS(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.inj_err_dom;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'inj_err_dlc' field in the 'STATUS_FLAGS' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_STATUS_FLAGS_inj_err_dlc_equal(
    signal net : inout network_t;
    expected : in std_ulogic;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'inj_err_dlc' field in the 'STATUS_FLAGS' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_STATUS_FLAGS_t := can_fd_fi_STATUS_FLAGS_init;
    variable got : std_ulogic := can_fd_fi_STATUS_FLAGS_inj_err_dlc_init;
  begin
    read_can_fd_fi_STATUS_FLAGS(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.inj_err_dlc;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'inj_err_fsm' field in the 'STATUS_FLAGS' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_STATUS_FLAGS_inj_err_fsm_equal(
    signal net : inout network_t;
    expected : in std_ulogic;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'inj_err_fsm' field in the 'STATUS_FLAGS' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_STATUS_FLAGS_t := can_fd_fi_STATUS_FLAGS_init;
    variable got : std_ulogic := can_fd_fi_STATUS_FLAGS_inj_err_fsm_init;
  begin
    read_can_fd_fi_STATUS_FLAGS(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.inj_err_fsm;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'GLOBAL_CONFIG' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_GLOBAL_CONFIG_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'GLOBAL_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_GLOBAL_CONFIG(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'GLOBAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_GLOBAL_CONFIG_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_GLOBAL_CONFIG_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'GLOBAL_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_GLOBAL_CONFIG_t;
  begin
    read_can_fd_fi_GLOBAL_CONFIG(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'cfg_en' field in the 'GLOBAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_GLOBAL_CONFIG_cfg_en_equal(
    signal net : inout network_t;
    expected : in std_ulogic;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'cfg_en' field in the 'GLOBAL_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_GLOBAL_CONFIG_t := can_fd_fi_GLOBAL_CONFIG_init;
    variable got : std_ulogic := can_fd_fi_GLOBAL_CONFIG_cfg_en_init;
  begin
    read_can_fd_fi_GLOBAL_CONFIG(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.cfg_en;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'cfg_brp_nomi' field in the 'GLOBAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_GLOBAL_CONFIG_cfg_brp_nomi_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_GLOBAL_CONFIG_cfg_brp_nomi_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'cfg_brp_nomi' field in the 'GLOBAL_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_GLOBAL_CONFIG_t := can_fd_fi_GLOBAL_CONFIG_init;
    variable got : can_fd_fi_GLOBAL_CONFIG_cfg_brp_nomi_t := can_fd_fi_GLOBAL_CONFIG_cfg_brp_nomi_init;
  begin
    read_can_fd_fi_GLOBAL_CONFIG(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.cfg_brp_nomi;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'cfg_brp_data' field in the 'GLOBAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_GLOBAL_CONFIG_cfg_brp_data_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_GLOBAL_CONFIG_cfg_brp_data_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'cfg_brp_data' field in the 'GLOBAL_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_GLOBAL_CONFIG_t := can_fd_fi_GLOBAL_CONFIG_init;
    variable got : can_fd_fi_GLOBAL_CONFIG_cfg_brp_data_t := can_fd_fi_GLOBAL_CONFIG_cfg_brp_data_init;
  begin
    read_can_fd_fi_GLOBAL_CONFIG(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.cfg_brp_data;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'cfg_td_en' field in the 'GLOBAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_GLOBAL_CONFIG_cfg_td_en_equal(
    signal net : inout network_t;
    expected : in std_ulogic;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'cfg_td_en' field in the 'GLOBAL_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_GLOBAL_CONFIG_t := can_fd_fi_GLOBAL_CONFIG_init;
    variable got : std_ulogic := can_fd_fi_GLOBAL_CONFIG_cfg_td_en_init;
  begin
    read_can_fd_fi_GLOBAL_CONFIG(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.cfg_td_en;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'cfg_ssp' field in the 'GLOBAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_GLOBAL_CONFIG_cfg_ssp_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_GLOBAL_CONFIG_cfg_ssp_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'cfg_ssp' field in the 'GLOBAL_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_GLOBAL_CONFIG_t := can_fd_fi_GLOBAL_CONFIG_init;
    variable got : can_fd_fi_GLOBAL_CONFIG_cfg_ssp_t := can_fd_fi_GLOBAL_CONFIG_cfg_ssp_init;
  begin
    read_can_fd_fi_GLOBAL_CONFIG(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.cfg_ssp;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'NOMINAL_CONFIG' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_NOMINAL_CONFIG_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'NOMINAL_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_NOMINAL_CONFIG(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'NOMINAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_NOMINAL_CONFIG_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_NOMINAL_CONFIG_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'NOMINAL_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_NOMINAL_CONFIG_t;
  begin
    read_can_fd_fi_NOMINAL_CONFIG(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'cfg_ph1_len_nomi' field in the 'NOMINAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_NOMINAL_CONFIG_cfg_ph1_len_nomi_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_NOMINAL_CONFIG_cfg_ph1_len_nomi_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'cfg_ph1_len_nomi' field in the 'NOMINAL_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_NOMINAL_CONFIG_t := can_fd_fi_NOMINAL_CONFIG_init;
    variable got : can_fd_fi_NOMINAL_CONFIG_cfg_ph1_len_nomi_t := can_fd_fi_NOMINAL_CONFIG_cfg_ph1_len_nomi_init;
  begin
    read_can_fd_fi_NOMINAL_CONFIG(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.cfg_ph1_len_nomi;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'cfg_ph2_len_nomi' field in the 'NOMINAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_NOMINAL_CONFIG_cfg_ph2_len_nomi_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_NOMINAL_CONFIG_cfg_ph2_len_nomi_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'cfg_ph2_len_nomi' field in the 'NOMINAL_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_NOMINAL_CONFIG_t := can_fd_fi_NOMINAL_CONFIG_init;
    variable got : can_fd_fi_NOMINAL_CONFIG_cfg_ph2_len_nomi_t := can_fd_fi_NOMINAL_CONFIG_cfg_ph2_len_nomi_init;
  begin
    read_can_fd_fi_NOMINAL_CONFIG(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.cfg_ph2_len_nomi;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'cfg_sjw_nomi' field in the 'NOMINAL_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_NOMINAL_CONFIG_cfg_sjw_nomi_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_NOMINAL_CONFIG_cfg_sjw_nomi_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'cfg_sjw_nomi' field in the 'NOMINAL_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_NOMINAL_CONFIG_t := can_fd_fi_NOMINAL_CONFIG_init;
    variable got : can_fd_fi_NOMINAL_CONFIG_cfg_sjw_nomi_t := can_fd_fi_NOMINAL_CONFIG_cfg_sjw_nomi_init;
  begin
    read_can_fd_fi_NOMINAL_CONFIG(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.cfg_sjw_nomi;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'DATA_CONFIG' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_DATA_CONFIG_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'DATA_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_DATA_CONFIG(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'DATA_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_DATA_CONFIG_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_DATA_CONFIG_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'DATA_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_DATA_CONFIG_t;
  begin
    read_can_fd_fi_DATA_CONFIG(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'cfg_ph1_len_data' field in the 'DATA_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_DATA_CONFIG_cfg_ph1_len_data_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_DATA_CONFIG_cfg_ph1_len_data_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'cfg_ph1_len_data' field in the 'DATA_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_DATA_CONFIG_t := can_fd_fi_DATA_CONFIG_init;
    variable got : can_fd_fi_DATA_CONFIG_cfg_ph1_len_data_t := can_fd_fi_DATA_CONFIG_cfg_ph1_len_data_init;
  begin
    read_can_fd_fi_DATA_CONFIG(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.cfg_ph1_len_data;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'cfg_ph2_len_data' field in the 'DATA_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_DATA_CONFIG_cfg_ph2_len_data_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_DATA_CONFIG_cfg_ph2_len_data_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'cfg_ph2_len_data' field in the 'DATA_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_DATA_CONFIG_t := can_fd_fi_DATA_CONFIG_init;
    variable got : can_fd_fi_DATA_CONFIG_cfg_ph2_len_data_t := can_fd_fi_DATA_CONFIG_cfg_ph2_len_data_init;
  begin
    read_can_fd_fi_DATA_CONFIG(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.cfg_ph2_len_data;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'cfg_sjw_data' field in the 'DATA_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_DATA_CONFIG_cfg_sjw_data_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_DATA_CONFIG_cfg_sjw_data_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'cfg_sjw_data' field in the 'DATA_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_DATA_CONFIG_t := can_fd_fi_DATA_CONFIG_init;
    variable got : can_fd_fi_DATA_CONFIG_cfg_sjw_data_t := can_fd_fi_DATA_CONFIG_cfg_sjw_data_init;
  begin
    read_can_fd_fi_DATA_CONFIG(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.cfg_sjw_data;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'FI_CONFIG' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_FI_CONFIG_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'FI_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_FI_CONFIG(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'FI_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_FI_CONFIG_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_FI_CONFIG_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'FI_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_FI_CONFIG_t;
  begin
    read_can_fd_fi_FI_CONFIG(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'cfg_psp_nomi' field in the 'FI_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_FI_CONFIG_cfg_psp_nomi_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_FI_CONFIG_cfg_psp_nomi_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'cfg_psp_nomi' field in the 'FI_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_FI_CONFIG_t := can_fd_fi_FI_CONFIG_init;
    variable got : can_fd_fi_FI_CONFIG_cfg_psp_nomi_t := can_fd_fi_FI_CONFIG_cfg_psp_nomi_init;
  begin
    read_can_fd_fi_FI_CONFIG(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.cfg_psp_nomi;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'cfg_psp_data' field in the 'FI_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_FI_CONFIG_cfg_psp_data_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_FI_CONFIG_cfg_psp_data_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'cfg_psp_data' field in the 'FI_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_FI_CONFIG_t := can_fd_fi_FI_CONFIG_init;
    variable got : can_fd_fi_FI_CONFIG_cfg_psp_data_t := can_fd_fi_FI_CONFIG_cfg_psp_data_init;
  begin
    read_can_fd_fi_FI_CONFIG(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.cfg_psp_data;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'cfg_fip_nomi' field in the 'FI_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_FI_CONFIG_cfg_fip_nomi_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_FI_CONFIG_cfg_fip_nomi_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'cfg_fip_nomi' field in the 'FI_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_FI_CONFIG_t := can_fd_fi_FI_CONFIG_init;
    variable got : can_fd_fi_FI_CONFIG_cfg_fip_nomi_t := can_fd_fi_FI_CONFIG_cfg_fip_nomi_init;
  begin
    read_can_fd_fi_FI_CONFIG(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.cfg_fip_nomi;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'cfg_fip_data' field in the 'FI_CONFIG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_FI_CONFIG_cfg_fip_data_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_FI_CONFIG_cfg_fip_data_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'cfg_fip_data' field in the 'FI_CONFIG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_FI_CONFIG_t := can_fd_fi_FI_CONFIG_init;
    variable got : can_fd_fi_FI_CONFIG_cfg_fip_data_t := can_fd_fi_FI_CONFIG_cfg_fip_data_init;
  begin
    read_can_fd_fi_FI_CONFIG(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.cfg_fip_data;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'IRQ_RST_REG' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_IRQ_RST_REG_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'IRQ_RST_REG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_IRQ_RST_REG(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'IRQ_RST_REG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_IRQ_RST_REG_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_IRQ_RST_REG_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'IRQ_RST_REG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_IRQ_RST_REG_t;
  begin
    read_can_fd_fi_IRQ_RST_REG(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'irq_rst' field in the 'IRQ_RST_REG' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_IRQ_RST_REG_irq_rst_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_IRQ_RST_REG_irq_rst_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'irq_rst' field in the 'IRQ_RST_REG' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_IRQ_RST_REG_t := can_fd_fi_IRQ_RST_REG_init;
    variable got : can_fd_fi_IRQ_RST_REG_irq_rst_t := can_fd_fi_IRQ_RST_REG_irq_rst_init;
  begin
    read_can_fd_fi_IRQ_RST_REG(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.irq_rst;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM0_MATCH' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM0_MATCH_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM0_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM0_MATCH(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM0_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM0_MATCH_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM0_MATCH_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM0_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM0_MATCH_t;
  begin
    read_can_fd_fi_ID_MEM0_MATCH(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm0_match' field in the 'ID_MEM0_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM0_MATCH_idm0_match_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM0_MATCH_idm0_match_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm0_match' field in the 'ID_MEM0_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM0_MATCH_t := can_fd_fi_ID_MEM0_MATCH_init;
    variable got : can_fd_fi_ID_MEM0_MATCH_idm0_match_t := can_fd_fi_ID_MEM0_MATCH_idm0_match_init;
  begin
    read_can_fd_fi_ID_MEM0_MATCH(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm0_match;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM0_MASK' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM0_MASK_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM0_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM0_MASK(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM0_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM0_MASK_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM0_MASK_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM0_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM0_MASK_t;
  begin
    read_can_fd_fi_ID_MEM0_MASK(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm0_mask' field in the 'ID_MEM0_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM0_MASK_idm0_mask_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM0_MASK_idm0_mask_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm0_mask' field in the 'ID_MEM0_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM0_MASK_t := can_fd_fi_ID_MEM0_MASK_init;
    variable got : can_fd_fi_ID_MEM0_MASK_idm0_mask_t := can_fd_fi_ID_MEM0_MASK_idm0_mask_init;
  begin
    read_can_fd_fi_ID_MEM0_MASK(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm0_mask;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM0_FIF' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM0_FIF_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM0_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM0_FIF(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM0_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM0_FIF_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM0_FIF_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM0_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM0_FIF_t;
  begin
    read_can_fd_fi_ID_MEM0_FIF(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm0_fif' field in the 'ID_MEM0_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM0_FIF_idm0_fif_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM0_FIF_idm0_fif_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm0_fif' field in the 'ID_MEM0_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM0_FIF_t := can_fd_fi_ID_MEM0_FIF_init;
    variable got : can_fd_fi_ID_MEM0_FIF_idm0_fif_t := can_fd_fi_ID_MEM0_FIF_idm0_fif_init;
  begin
    read_can_fd_fi_ID_MEM0_FIF(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm0_fif;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM1_MATCH' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM1_MATCH_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM1_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM1_MATCH(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM1_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM1_MATCH_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM1_MATCH_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM1_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM1_MATCH_t;
  begin
    read_can_fd_fi_ID_MEM1_MATCH(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm1_match' field in the 'ID_MEM1_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM1_MATCH_idm1_match_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM1_MATCH_idm1_match_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm1_match' field in the 'ID_MEM1_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM1_MATCH_t := can_fd_fi_ID_MEM1_MATCH_init;
    variable got : can_fd_fi_ID_MEM1_MATCH_idm1_match_t := can_fd_fi_ID_MEM1_MATCH_idm1_match_init;
  begin
    read_can_fd_fi_ID_MEM1_MATCH(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm1_match;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM1_MASK' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM1_MASK_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM1_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM1_MASK(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM1_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM1_MASK_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM1_MASK_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM1_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM1_MASK_t;
  begin
    read_can_fd_fi_ID_MEM1_MASK(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm1_mask' field in the 'ID_MEM1_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM1_MASK_idm1_mask_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM1_MASK_idm1_mask_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm1_mask' field in the 'ID_MEM1_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM1_MASK_t := can_fd_fi_ID_MEM1_MASK_init;
    variable got : can_fd_fi_ID_MEM1_MASK_idm1_mask_t := can_fd_fi_ID_MEM1_MASK_idm1_mask_init;
  begin
    read_can_fd_fi_ID_MEM1_MASK(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm1_mask;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM1_FIF' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM1_FIF_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM1_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM1_FIF(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM1_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM1_FIF_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM1_FIF_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM1_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM1_FIF_t;
  begin
    read_can_fd_fi_ID_MEM1_FIF(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm1_fif' field in the 'ID_MEM1_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM1_FIF_idm1_fif_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM1_FIF_idm1_fif_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm1_fif' field in the 'ID_MEM1_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM1_FIF_t := can_fd_fi_ID_MEM1_FIF_init;
    variable got : can_fd_fi_ID_MEM1_FIF_idm1_fif_t := can_fd_fi_ID_MEM1_FIF_idm1_fif_init;
  begin
    read_can_fd_fi_ID_MEM1_FIF(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm1_fif;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM2_MATCH' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM2_MATCH_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM2_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM2_MATCH(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM2_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM2_MATCH_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM2_MATCH_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM2_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM2_MATCH_t;
  begin
    read_can_fd_fi_ID_MEM2_MATCH(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm2_match' field in the 'ID_MEM2_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM2_MATCH_idm2_match_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM2_MATCH_idm2_match_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm2_match' field in the 'ID_MEM2_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM2_MATCH_t := can_fd_fi_ID_MEM2_MATCH_init;
    variable got : can_fd_fi_ID_MEM2_MATCH_idm2_match_t := can_fd_fi_ID_MEM2_MATCH_idm2_match_init;
  begin
    read_can_fd_fi_ID_MEM2_MATCH(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm2_match;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM2_MASK' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM2_MASK_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM2_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM2_MASK(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM2_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM2_MASK_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM2_MASK_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM2_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM2_MASK_t;
  begin
    read_can_fd_fi_ID_MEM2_MASK(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm2_mask' field in the 'ID_MEM2_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM2_MASK_idm2_mask_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM2_MASK_idm2_mask_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm2_mask' field in the 'ID_MEM2_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM2_MASK_t := can_fd_fi_ID_MEM2_MASK_init;
    variable got : can_fd_fi_ID_MEM2_MASK_idm2_mask_t := can_fd_fi_ID_MEM2_MASK_idm2_mask_init;
  begin
    read_can_fd_fi_ID_MEM2_MASK(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm2_mask;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM2_FIF' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM2_FIF_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM2_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM2_FIF(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM2_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM2_FIF_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM2_FIF_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM2_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM2_FIF_t;
  begin
    read_can_fd_fi_ID_MEM2_FIF(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm2_fif' field in the 'ID_MEM2_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM2_FIF_idm2_fif_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM2_FIF_idm2_fif_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm2_fif' field in the 'ID_MEM2_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM2_FIF_t := can_fd_fi_ID_MEM2_FIF_init;
    variable got : can_fd_fi_ID_MEM2_FIF_idm2_fif_t := can_fd_fi_ID_MEM2_FIF_idm2_fif_init;
  begin
    read_can_fd_fi_ID_MEM2_FIF(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm2_fif;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM3_MATCH' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM3_MATCH_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM3_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM3_MATCH(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM3_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM3_MATCH_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM3_MATCH_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM3_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM3_MATCH_t;
  begin
    read_can_fd_fi_ID_MEM3_MATCH(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm3_match' field in the 'ID_MEM3_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM3_MATCH_idm3_match_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM3_MATCH_idm3_match_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm3_match' field in the 'ID_MEM3_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM3_MATCH_t := can_fd_fi_ID_MEM3_MATCH_init;
    variable got : can_fd_fi_ID_MEM3_MATCH_idm3_match_t := can_fd_fi_ID_MEM3_MATCH_idm3_match_init;
  begin
    read_can_fd_fi_ID_MEM3_MATCH(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm3_match;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM3_MASK' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM3_MASK_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM3_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM3_MASK(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM3_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM3_MASK_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM3_MASK_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM3_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM3_MASK_t;
  begin
    read_can_fd_fi_ID_MEM3_MASK(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm3_mask' field in the 'ID_MEM3_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM3_MASK_idm3_mask_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM3_MASK_idm3_mask_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm3_mask' field in the 'ID_MEM3_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM3_MASK_t := can_fd_fi_ID_MEM3_MASK_init;
    variable got : can_fd_fi_ID_MEM3_MASK_idm3_mask_t := can_fd_fi_ID_MEM3_MASK_idm3_mask_init;
  begin
    read_can_fd_fi_ID_MEM3_MASK(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm3_mask;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM3_FIF' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM3_FIF_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM3_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM3_FIF(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM3_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM3_FIF_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM3_FIF_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM3_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM3_FIF_t;
  begin
    read_can_fd_fi_ID_MEM3_FIF(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm3_fif' field in the 'ID_MEM3_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM3_FIF_idm3_fif_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM3_FIF_idm3_fif_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm3_fif' field in the 'ID_MEM3_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM3_FIF_t := can_fd_fi_ID_MEM3_FIF_init;
    variable got : can_fd_fi_ID_MEM3_FIF_idm3_fif_t := can_fd_fi_ID_MEM3_FIF_idm3_fif_init;
  begin
    read_can_fd_fi_ID_MEM3_FIF(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm3_fif;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM4_MATCH' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM4_MATCH_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM4_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM4_MATCH(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM4_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM4_MATCH_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM4_MATCH_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM4_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM4_MATCH_t;
  begin
    read_can_fd_fi_ID_MEM4_MATCH(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm4_match' field in the 'ID_MEM4_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM4_MATCH_idm4_match_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM4_MATCH_idm4_match_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm4_match' field in the 'ID_MEM4_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM4_MATCH_t := can_fd_fi_ID_MEM4_MATCH_init;
    variable got : can_fd_fi_ID_MEM4_MATCH_idm4_match_t := can_fd_fi_ID_MEM4_MATCH_idm4_match_init;
  begin
    read_can_fd_fi_ID_MEM4_MATCH(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm4_match;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM4_MASK' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM4_MASK_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM4_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM4_MASK(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM4_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM4_MASK_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM4_MASK_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM4_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM4_MASK_t;
  begin
    read_can_fd_fi_ID_MEM4_MASK(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm4_mask' field in the 'ID_MEM4_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM4_MASK_idm4_mask_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM4_MASK_idm4_mask_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm4_mask' field in the 'ID_MEM4_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM4_MASK_t := can_fd_fi_ID_MEM4_MASK_init;
    variable got : can_fd_fi_ID_MEM4_MASK_idm4_mask_t := can_fd_fi_ID_MEM4_MASK_idm4_mask_init;
  begin
    read_can_fd_fi_ID_MEM4_MASK(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm4_mask;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM4_FIF' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM4_FIF_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM4_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM4_FIF(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM4_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM4_FIF_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM4_FIF_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM4_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM4_FIF_t;
  begin
    read_can_fd_fi_ID_MEM4_FIF(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm4_fif' field in the 'ID_MEM4_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM4_FIF_idm4_fif_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM4_FIF_idm4_fif_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm4_fif' field in the 'ID_MEM4_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM4_FIF_t := can_fd_fi_ID_MEM4_FIF_init;
    variable got : can_fd_fi_ID_MEM4_FIF_idm4_fif_t := can_fd_fi_ID_MEM4_FIF_idm4_fif_init;
  begin
    read_can_fd_fi_ID_MEM4_FIF(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm4_fif;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM5_MATCH' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM5_MATCH_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM5_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM5_MATCH(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM5_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM5_MATCH_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM5_MATCH_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM5_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM5_MATCH_t;
  begin
    read_can_fd_fi_ID_MEM5_MATCH(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm5_match' field in the 'ID_MEM5_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM5_MATCH_idm5_match_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM5_MATCH_idm5_match_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm5_match' field in the 'ID_MEM5_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM5_MATCH_t := can_fd_fi_ID_MEM5_MATCH_init;
    variable got : can_fd_fi_ID_MEM5_MATCH_idm5_match_t := can_fd_fi_ID_MEM5_MATCH_idm5_match_init;
  begin
    read_can_fd_fi_ID_MEM5_MATCH(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm5_match;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM5_MASK' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM5_MASK_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM5_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM5_MASK(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM5_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM5_MASK_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM5_MASK_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM5_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM5_MASK_t;
  begin
    read_can_fd_fi_ID_MEM5_MASK(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm5_mask' field in the 'ID_MEM5_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM5_MASK_idm5_mask_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM5_MASK_idm5_mask_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm5_mask' field in the 'ID_MEM5_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM5_MASK_t := can_fd_fi_ID_MEM5_MASK_init;
    variable got : can_fd_fi_ID_MEM5_MASK_idm5_mask_t := can_fd_fi_ID_MEM5_MASK_idm5_mask_init;
  begin
    read_can_fd_fi_ID_MEM5_MASK(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm5_mask;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM5_FIF' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM5_FIF_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM5_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM5_FIF(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM5_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM5_FIF_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM5_FIF_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM5_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM5_FIF_t;
  begin
    read_can_fd_fi_ID_MEM5_FIF(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm5_fif' field in the 'ID_MEM5_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM5_FIF_idm5_fif_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM5_FIF_idm5_fif_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm5_fif' field in the 'ID_MEM5_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM5_FIF_t := can_fd_fi_ID_MEM5_FIF_init;
    variable got : can_fd_fi_ID_MEM5_FIF_idm5_fif_t := can_fd_fi_ID_MEM5_FIF_idm5_fif_init;
  begin
    read_can_fd_fi_ID_MEM5_FIF(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm5_fif;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM6_MATCH' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM6_MATCH_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM6_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM6_MATCH(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM6_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM6_MATCH_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM6_MATCH_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM6_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM6_MATCH_t;
  begin
    read_can_fd_fi_ID_MEM6_MATCH(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm6_match' field in the 'ID_MEM6_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM6_MATCH_idm6_match_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM6_MATCH_idm6_match_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm6_match' field in the 'ID_MEM6_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM6_MATCH_t := can_fd_fi_ID_MEM6_MATCH_init;
    variable got : can_fd_fi_ID_MEM6_MATCH_idm6_match_t := can_fd_fi_ID_MEM6_MATCH_idm6_match_init;
  begin
    read_can_fd_fi_ID_MEM6_MATCH(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm6_match;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM6_MASK' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM6_MASK_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM6_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM6_MASK(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM6_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM6_MASK_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM6_MASK_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM6_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM6_MASK_t;
  begin
    read_can_fd_fi_ID_MEM6_MASK(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm6_mask' field in the 'ID_MEM6_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM6_MASK_idm6_mask_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM6_MASK_idm6_mask_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm6_mask' field in the 'ID_MEM6_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM6_MASK_t := can_fd_fi_ID_MEM6_MASK_init;
    variable got : can_fd_fi_ID_MEM6_MASK_idm6_mask_t := can_fd_fi_ID_MEM6_MASK_idm6_mask_init;
  begin
    read_can_fd_fi_ID_MEM6_MASK(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm6_mask;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM6_FIF' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM6_FIF_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM6_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM6_FIF(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM6_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM6_FIF_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM6_FIF_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM6_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM6_FIF_t;
  begin
    read_can_fd_fi_ID_MEM6_FIF(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm6_fif' field in the 'ID_MEM6_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM6_FIF_idm6_fif_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM6_FIF_idm6_fif_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm6_fif' field in the 'ID_MEM6_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM6_FIF_t := can_fd_fi_ID_MEM6_FIF_init;
    variable got : can_fd_fi_ID_MEM6_FIF_idm6_fif_t := can_fd_fi_ID_MEM6_FIF_idm6_fif_init;
  begin
    read_can_fd_fi_ID_MEM6_FIF(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm6_fif;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM7_MATCH' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM7_MATCH_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM7_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM7_MATCH(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM7_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM7_MATCH_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM7_MATCH_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM7_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM7_MATCH_t;
  begin
    read_can_fd_fi_ID_MEM7_MATCH(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm7_match' field in the 'ID_MEM7_MATCH' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM7_MATCH_idm7_match_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM7_MATCH_idm7_match_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm7_match' field in the 'ID_MEM7_MATCH' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM7_MATCH_t := can_fd_fi_ID_MEM7_MATCH_init;
    variable got : can_fd_fi_ID_MEM7_MATCH_idm7_match_t := can_fd_fi_ID_MEM7_MATCH_idm7_match_init;
  begin
    read_can_fd_fi_ID_MEM7_MATCH(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm7_match;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM7_MASK' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM7_MASK_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM7_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM7_MASK(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM7_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM7_MASK_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM7_MASK_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM7_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM7_MASK_t;
  begin
    read_can_fd_fi_ID_MEM7_MASK(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm7_mask' field in the 'ID_MEM7_MASK' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM7_MASK_idm7_mask_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM7_MASK_idm7_mask_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm7_mask' field in the 'ID_MEM7_MASK' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM7_MASK_t := can_fd_fi_ID_MEM7_MASK_init;
    variable got : can_fd_fi_ID_MEM7_MASK_idm7_mask_t := can_fd_fi_ID_MEM7_MASK_idm7_mask_init;
  begin
    read_can_fd_fi_ID_MEM7_MASK(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm7_mask;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

  -- ---------------------------------------------------------------------------
  -- Check that the current value of the 'ID_MEM7_FIF' register
  -- equals the given 'expected' value as a plain SLV casted to integer.
  procedure check_can_fd_fi_ID_MEM7_FIF_equal(
    signal net : inout network_t;
    expected : in integer;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM7_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : integer;
  begin
    read_can_fd_fi_ID_MEM7_FIF(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;

  -- Check that the current value of the 'ID_MEM7_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM7_FIF_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM7_FIF_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'ID_MEM7_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got : can_fd_fi_ID_MEM7_FIF_t;
  begin
    read_can_fd_fi_ID_MEM7_FIF(
      net => net,
      value => got,
      base_address => base_address,
      bus_handle => bus_handle
    );

    if got /= expected then
      failing_check(
        checker => default_checker,
        msg => p_std_msg(
          check_result => "Equality check failed",
          msg => get_message,
          ctx => (
            "Got " & to_string(to_slv(got)) & ". Expected " & to_string(to_slv(expected)) & "."
          )
        )
      );
    end if;
  end procedure;

  -- Check that the current value of the 'idm7_fif' field in the 'ID_MEM7_FIF' register
  -- equals the given 'expected' value.
  procedure check_can_fd_fi_ID_MEM7_FIF_idm7_fif_equal(
    signal net : inout network_t;
    expected : in can_fd_fi_ID_MEM7_FIF_idm7_fif_t;
    base_address : in unsigned(32 - 1 downto 0) := (others => '0');
    bus_handle : in bus_master_t := register_bus_master;
    message : in string := ""
  ) is
    constant register_array_message : string := "";
    function base_address_message return string is
    begin
      if base_address /= 0 then
        return " (at base address " & hex_image(std_logic_vector(base_address)) & ")";
      end if;

      return "";
    end function;
    constant base_message : string := (
      "Checking the 'idm7_fif' field in the 'ID_MEM7_FIF' register"
      & register_array_message
      & base_address_message
      & "."
    );
    function get_message return string is
    begin
      if message = "" then
        return base_message;
      end if;

      return base_message & " " & message & ".";
    end function;

    variable got_reg : can_fd_fi_ID_MEM7_FIF_t := can_fd_fi_ID_MEM7_FIF_init;
    variable got : can_fd_fi_ID_MEM7_FIF_idm7_fif_t := can_fd_fi_ID_MEM7_FIF_idm7_fif_init;
  begin
    read_can_fd_fi_ID_MEM7_FIF(
      net => net,
      value => got_reg,
      base_address => base_address,
      bus_handle => bus_handle
    );
    got := got_reg.idm7_fif;

    check_equal(got=>got, expected=>expected, msg=>get_message);
  end procedure;
  -- ---------------------------------------------------------------------------

end package body;
