// -----------------------------------------------------------------------------
// This file is automatically generated by hdl-registers version 8.1.0.
// Code generator CppInterfaceGenerator version 1.0.0.
// -----------------------------------------------------------------------------

#pragma once

#include <sstream>
#include <cstdint>
#include <cstdlib>

namespace fpga_regs
{

  namespace can_fd_fi
  {

    // Attributes for the 'CORE_INFO' register.
    namespace CORE_INFO {
      // Attributes for the 'info_t' field.
      namespace info_t
      {
        // The number of bits that the field occupies.
        static const size_t width = 32;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 1246644547;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b01001010010011100100100101000011uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t info_t;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        info_t::default_value,
      };
    }

    // Attributes for the 'CORE_VERSION' register.
    namespace CORE_VERSION {
      // Attributes for the 'magic_valid' field.
      namespace magic_valid
      {
        // The number of bits that the field occupies.
        static const size_t width = 8;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 170;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b10101010uL << shift;
      }

      // Attributes for the 'patch' field.
      namespace patch
      {
        // The number of bits that the field occupies.
        static const size_t width = 8;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 8;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 5;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000101uL << shift;
      }

      // Attributes for the 'minor' field.
      namespace minor
      {
        // The number of bits that the field occupies.
        static const size_t width = 8;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 16;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 2;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000010uL << shift;
      }

      // Attributes for the 'major' field.
      namespace major
      {
        // The number of bits that the field occupies.
        static const size_t width = 8;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 24;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 1;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000001uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t magic_valid;
        uint32_t patch;
        uint32_t minor;
        uint32_t major;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        magic_valid::default_value,
        patch::default_value,
        minor::default_value,
        major::default_value,
      };
    }

    // Attributes for the 'STATUS_FLAGS' register.
    namespace STATUS_FLAGS {
      // Attributes for the 'tu_ready_config' field.
      namespace tu_ready_config
      {
        // The number of bits that the field occupies.
        static const size_t width = 1;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const bool default_value = true;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 1 << shift;
      }

      // Attributes for the 'inj_err_miss' field.
      namespace inj_err_miss
      {
        // The number of bits that the field occupies.
        static const size_t width = 1;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 1;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const bool default_value = false;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0 << shift;
      }

      // Attributes for the 'inj_err_dom' field.
      namespace inj_err_dom
      {
        // The number of bits that the field occupies.
        static const size_t width = 1;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 2;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const bool default_value = false;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0 << shift;
      }

      // Attributes for the 'inj_err_dlc' field.
      namespace inj_err_dlc
      {
        // The number of bits that the field occupies.
        static const size_t width = 1;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 3;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const bool default_value = false;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0 << shift;
      }

      // Attributes for the 'inj_err_fsm' field.
      namespace inj_err_fsm
      {
        // The number of bits that the field occupies.
        static const size_t width = 1;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 4;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const bool default_value = false;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0 << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        bool tu_ready_config;
        bool inj_err_miss;
        bool inj_err_dom;
        bool inj_err_dlc;
        bool inj_err_fsm;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        tu_ready_config::default_value,
        inj_err_miss::default_value,
        inj_err_dom::default_value,
        inj_err_dlc::default_value,
        inj_err_fsm::default_value,
      };
    }

    // Attributes for the 'GLOBAL_CONFIG' register.
    namespace GLOBAL_CONFIG {
      // Attributes for the 'cfg_en' field.
      namespace cfg_en
      {
        // The number of bits that the field occupies.
        static const size_t width = 1;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const bool default_value = false;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0 << shift;
      }

      // Attributes for the 'cfg_brp_nomi' field.
      namespace cfg_brp_nomi
      {
        // The number of bits that the field occupies.
        static const size_t width = 6;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 1;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 8;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 8uL << shift;
      }

      // Attributes for the 'cfg_brp_data' field.
      namespace cfg_brp_data
      {
        // The number of bits that the field occupies.
        static const size_t width = 6;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 7;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 8;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 8uL << shift;
      }

      // Attributes for the 'cfg_td_en' field.
      namespace cfg_td_en
      {
        // The number of bits that the field occupies.
        static const size_t width = 1;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 13;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const bool default_value = false;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0 << shift;
      }

      // Attributes for the 'cfg_ssp' field.
      namespace cfg_ssp
      {
        // The number of bits that the field occupies.
        static const size_t width = 9;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 14;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        bool cfg_en;
        uint32_t cfg_brp_nomi;
        uint32_t cfg_brp_data;
        bool cfg_td_en;
        uint32_t cfg_ssp;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        cfg_en::default_value,
        cfg_brp_nomi::default_value,
        cfg_brp_data::default_value,
        cfg_td_en::default_value,
        cfg_ssp::default_value,
      };
    }

    // Attributes for the 'NOMINAL_CONFIG' register.
    namespace NOMINAL_CONFIG {
      // Attributes for the 'cfg_ph1_len_nomi' field.
      namespace cfg_ph1_len_nomi
      {
        // The number of bits that the field occupies.
        static const size_t width = 8;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 1;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 1uL << shift;
      }

      // Attributes for the 'cfg_ph2_len_nomi' field.
      namespace cfg_ph2_len_nomi
      {
        // The number of bits that the field occupies.
        static const size_t width = 7;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 8;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 1;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 1uL << shift;
      }

      // Attributes for the 'cfg_sjw_nomi' field.
      namespace cfg_sjw_nomi
      {
        // The number of bits that the field occupies.
        static const size_t width = 7;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 15;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 1;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 1uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t cfg_ph1_len_nomi;
        uint32_t cfg_ph2_len_nomi;
        uint32_t cfg_sjw_nomi;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        cfg_ph1_len_nomi::default_value,
        cfg_ph2_len_nomi::default_value,
        cfg_sjw_nomi::default_value,
      };
    }

    // Attributes for the 'DATA_CONFIG' register.
    namespace DATA_CONFIG {
      // Attributes for the 'cfg_ph1_len_data' field.
      namespace cfg_ph1_len_data
      {
        // The number of bits that the field occupies.
        static const size_t width = 8;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 1;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 1uL << shift;
      }

      // Attributes for the 'cfg_ph2_len_data' field.
      namespace cfg_ph2_len_data
      {
        // The number of bits that the field occupies.
        static const size_t width = 7;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 8;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 1;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 1uL << shift;
      }

      // Attributes for the 'cfg_sjw_data' field.
      namespace cfg_sjw_data
      {
        // The number of bits that the field occupies.
        static const size_t width = 7;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 15;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 1;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 1uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t cfg_ph1_len_data;
        uint32_t cfg_ph2_len_data;
        uint32_t cfg_sjw_data;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        cfg_ph1_len_data::default_value,
        cfg_ph2_len_data::default_value,
        cfg_sjw_data::default_value,
      };
    }

    // Attributes for the 'FI_CONFIG' register.
    namespace FI_CONFIG {
      // Attributes for the 'cfg_psp_nomi' field.
      namespace cfg_psp_nomi
      {
        // The number of bits that the field occupies.
        static const size_t width = 8;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0uL << shift;
      }

      // Attributes for the 'cfg_psp_data' field.
      namespace cfg_psp_data
      {
        // The number of bits that the field occupies.
        static const size_t width = 8;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 8;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0uL << shift;
      }

      // Attributes for the 'cfg_fip_nomi' field.
      namespace cfg_fip_nomi
      {
        // The number of bits that the field occupies.
        static const size_t width = 8;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 16;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0uL << shift;
      }

      // Attributes for the 'cfg_fip_data' field.
      namespace cfg_fip_data
      {
        // The number of bits that the field occupies.
        static const size_t width = 8;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 24;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t cfg_psp_nomi;
        uint32_t cfg_psp_data;
        uint32_t cfg_fip_nomi;
        uint32_t cfg_fip_data;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        cfg_psp_nomi::default_value,
        cfg_psp_data::default_value,
        cfg_fip_nomi::default_value,
        cfg_fip_data::default_value,
      };
    }

    // Attributes for the 'IRQ_RST_REG' register.
    namespace IRQ_RST_REG {
      // Attributes for the 'irq_rst' field.
      namespace irq_rst
      {
        // The number of bits that the field occupies.
        static const size_t width = 32;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t irq_rst;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        irq_rst::default_value,
      };
    }

    // Attributes for the 'ID_MEM0_MATCH' register.
    namespace ID_MEM0_MATCH {
      // Attributes for the 'idm0_match' field.
      namespace idm0_match
      {
        // The number of bits that the field occupies.
        static const size_t width = 29;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm0_match;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm0_match::default_value,
      };
    }

    // Attributes for the 'ID_MEM0_MASK' register.
    namespace ID_MEM0_MASK {
      // Attributes for the 'idm0_mask' field.
      namespace idm0_mask
      {
        // The number of bits that the field occupies.
        static const size_t width = 29;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm0_mask;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm0_mask::default_value,
      };
    }

    // Attributes for the 'ID_MEM0_FIF' register.
    namespace ID_MEM0_FIF {
      // Attributes for the 'idm0_fif' field.
      namespace idm0_fif
      {
        // The number of bits that the field occupies.
        static const size_t width = 18;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm0_fif;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm0_fif::default_value,
      };
    }

    // Attributes for the 'ID_MEM1_MATCH' register.
    namespace ID_MEM1_MATCH {
      // Attributes for the 'idm1_match' field.
      namespace idm1_match
      {
        // The number of bits that the field occupies.
        static const size_t width = 29;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm1_match;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm1_match::default_value,
      };
    }

    // Attributes for the 'ID_MEM1_MASK' register.
    namespace ID_MEM1_MASK {
      // Attributes for the 'idm1_mask' field.
      namespace idm1_mask
      {
        // The number of bits that the field occupies.
        static const size_t width = 29;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm1_mask;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm1_mask::default_value,
      };
    }

    // Attributes for the 'ID_MEM1_FIF' register.
    namespace ID_MEM1_FIF {
      // Attributes for the 'idm1_fif' field.
      namespace idm1_fif
      {
        // The number of bits that the field occupies.
        static const size_t width = 18;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm1_fif;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm1_fif::default_value,
      };
    }

    // Attributes for the 'ID_MEM2_MATCH' register.
    namespace ID_MEM2_MATCH {
      // Attributes for the 'idm2_match' field.
      namespace idm2_match
      {
        // The number of bits that the field occupies.
        static const size_t width = 29;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm2_match;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm2_match::default_value,
      };
    }

    // Attributes for the 'ID_MEM2_MASK' register.
    namespace ID_MEM2_MASK {
      // Attributes for the 'idm2_mask' field.
      namespace idm2_mask
      {
        // The number of bits that the field occupies.
        static const size_t width = 29;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm2_mask;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm2_mask::default_value,
      };
    }

    // Attributes for the 'ID_MEM2_FIF' register.
    namespace ID_MEM2_FIF {
      // Attributes for the 'idm2_fif' field.
      namespace idm2_fif
      {
        // The number of bits that the field occupies.
        static const size_t width = 18;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm2_fif;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm2_fif::default_value,
      };
    }

    // Attributes for the 'ID_MEM3_MATCH' register.
    namespace ID_MEM3_MATCH {
      // Attributes for the 'idm3_match' field.
      namespace idm3_match
      {
        // The number of bits that the field occupies.
        static const size_t width = 29;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm3_match;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm3_match::default_value,
      };
    }

    // Attributes for the 'ID_MEM3_MASK' register.
    namespace ID_MEM3_MASK {
      // Attributes for the 'idm3_mask' field.
      namespace idm3_mask
      {
        // The number of bits that the field occupies.
        static const size_t width = 29;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm3_mask;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm3_mask::default_value,
      };
    }

    // Attributes for the 'ID_MEM3_FIF' register.
    namespace ID_MEM3_FIF {
      // Attributes for the 'idm3_fif' field.
      namespace idm3_fif
      {
        // The number of bits that the field occupies.
        static const size_t width = 18;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm3_fif;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm3_fif::default_value,
      };
    }

    // Attributes for the 'ID_MEM4_MATCH' register.
    namespace ID_MEM4_MATCH {
      // Attributes for the 'idm4_match' field.
      namespace idm4_match
      {
        // The number of bits that the field occupies.
        static const size_t width = 29;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm4_match;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm4_match::default_value,
      };
    }

    // Attributes for the 'ID_MEM4_MASK' register.
    namespace ID_MEM4_MASK {
      // Attributes for the 'idm4_mask' field.
      namespace idm4_mask
      {
        // The number of bits that the field occupies.
        static const size_t width = 29;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm4_mask;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm4_mask::default_value,
      };
    }

    // Attributes for the 'ID_MEM4_FIF' register.
    namespace ID_MEM4_FIF {
      // Attributes for the 'idm4_fif' field.
      namespace idm4_fif
      {
        // The number of bits that the field occupies.
        static const size_t width = 18;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm4_fif;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm4_fif::default_value,
      };
    }

    // Attributes for the 'ID_MEM5_MATCH' register.
    namespace ID_MEM5_MATCH {
      // Attributes for the 'idm5_match' field.
      namespace idm5_match
      {
        // The number of bits that the field occupies.
        static const size_t width = 29;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm5_match;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm5_match::default_value,
      };
    }

    // Attributes for the 'ID_MEM5_MASK' register.
    namespace ID_MEM5_MASK {
      // Attributes for the 'idm5_mask' field.
      namespace idm5_mask
      {
        // The number of bits that the field occupies.
        static const size_t width = 29;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm5_mask;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm5_mask::default_value,
      };
    }

    // Attributes for the 'ID_MEM5_FIF' register.
    namespace ID_MEM5_FIF {
      // Attributes for the 'idm5_fif' field.
      namespace idm5_fif
      {
        // The number of bits that the field occupies.
        static const size_t width = 18;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm5_fif;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm5_fif::default_value,
      };
    }

    // Attributes for the 'ID_MEM6_MATCH' register.
    namespace ID_MEM6_MATCH {
      // Attributes for the 'idm6_match' field.
      namespace idm6_match
      {
        // The number of bits that the field occupies.
        static const size_t width = 29;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm6_match;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm6_match::default_value,
      };
    }

    // Attributes for the 'ID_MEM6_MASK' register.
    namespace ID_MEM6_MASK {
      // Attributes for the 'idm6_mask' field.
      namespace idm6_mask
      {
        // The number of bits that the field occupies.
        static const size_t width = 29;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm6_mask;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm6_mask::default_value,
      };
    }

    // Attributes for the 'ID_MEM6_FIF' register.
    namespace ID_MEM6_FIF {
      // Attributes for the 'idm6_fif' field.
      namespace idm6_fif
      {
        // The number of bits that the field occupies.
        static const size_t width = 18;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm6_fif;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm6_fif::default_value,
      };
    }

    // Attributes for the 'ID_MEM7_MATCH' register.
    namespace ID_MEM7_MATCH {
      // Attributes for the 'idm7_match' field.
      namespace idm7_match
      {
        // The number of bits that the field occupies.
        static const size_t width = 29;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm7_match;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm7_match::default_value,
      };
    }

    // Attributes for the 'ID_MEM7_MASK' register.
    namespace ID_MEM7_MASK {
      // Attributes for the 'idm7_mask' field.
      namespace idm7_mask
      {
        // The number of bits that the field occupies.
        static const size_t width = 29;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm7_mask;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm7_mask::default_value,
      };
    }

    // Attributes for the 'ID_MEM7_FIF' register.
    namespace ID_MEM7_FIF {
      // Attributes for the 'idm7_fif' field.
      namespace idm7_fif
      {
        // The number of bits that the field occupies.
        static const size_t width = 18;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t idm7_fif;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        idm7_fif::default_value,
      };
    }

    // Attributes for the 'MU_CFG_BASE' register.
    namespace MU_CFG_BASE {
      // Attributes for the 'ram_data_valid' field.
      namespace ram_data_valid
      {
        // The number of bits that the field occupies.
        static const size_t width = 1;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const bool default_value = false;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0 << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        bool ram_data_valid;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        ram_data_valid::default_value,
      };
    }

    // Attributes for the 'MU_CFG_ADDR' register.
    namespace MU_CFG_ADDR {
      // Attributes for the 'ram_data_address' field.
      namespace ram_data_address
      {
        // The number of bits that the field occupies.
        static const size_t width = 15;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 1;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b000000000000001uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t ram_data_address;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        ram_data_address::default_value,
      };
    }

    // Attributes for the 'MU_RAM_DATA' register.
    namespace MU_RAM_DATA {
      // Attributes for the 'ram_data_payload' field.
      namespace ram_data_payload
      {
        // The number of bits that the field occupies.
        static const size_t width = 32;
        // The bit index of the lowest bit in the field.
        static const size_t shift = 0;
        // The bit mask of the field, at index zero.
        static const uint32_t mask_at_base = (1uLL << width) - 1;
        // The bit mask of the field, at the field's bit index.
        static const uint32_t mask_shifted = mask_at_base << shift;

        // Initial value of the field at device startup/reset.
        static const uint32_t default_value = 0;
        // Raw representation of the initial value, at the the field's bit index.
        static const uint32_t default_value_raw = 0b00000000000000000000000000000000uL << shift;
      }

      // Struct that holds the value of each field in the register,
      // in a native C++ representation.
      struct Value {
        uint32_t ram_data_payload;
      };
      // Initial value of the register at device startup/reset.
      const Value default_value = {
        ram_data_payload::default_value,
      };
    }

  } // namespace can_fd_fi

  class ICanFdFi
  {
  public:
    // Number of registers within this register list.
    static const size_t num_registers = 35uL;

    virtual ~ICanFdFi() {}

    // -------------------------------------------------------------------------
    // Methods for the 'CORE_INFO' register.
    // Mode 'Read'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::CORE_INFO::Value get_CORE_INFO() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_CORE_INFO_raw() const = 0;

    // Read the register and slice out the 'info_t' field value.
    virtual uint32_t get_CORE_INFO_info_t() const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'CORE_VERSION' register.
    // Mode 'Read'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::CORE_VERSION::Value get_CORE_VERSION() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_CORE_VERSION_raw() const = 0;

    // Read the register and slice out the 'magic_valid' field value.
    virtual uint32_t get_CORE_VERSION_magic_valid() const = 0;

    // Read the register and slice out the 'patch' field value.
    virtual uint32_t get_CORE_VERSION_patch() const = 0;

    // Read the register and slice out the 'minor' field value.
    virtual uint32_t get_CORE_VERSION_minor() const = 0;

    // Read the register and slice out the 'major' field value.
    virtual uint32_t get_CORE_VERSION_major() const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'STATUS_FLAGS' register.
    // Mode 'Read'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::STATUS_FLAGS::Value get_STATUS_FLAGS() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_STATUS_FLAGS_raw() const = 0;

    // Read the register and slice out the 'tu_ready_config' field value.
    virtual bool get_STATUS_FLAGS_tu_ready_config() const = 0;

    // Read the register and slice out the 'inj_err_miss' field value.
    virtual bool get_STATUS_FLAGS_inj_err_miss() const = 0;

    // Read the register and slice out the 'inj_err_dom' field value.
    virtual bool get_STATUS_FLAGS_inj_err_dom() const = 0;

    // Read the register and slice out the 'inj_err_dlc' field value.
    virtual bool get_STATUS_FLAGS_inj_err_dlc() const = 0;

    // Read the register and slice out the 'inj_err_fsm' field value.
    virtual bool get_STATUS_FLAGS_inj_err_fsm() const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'GLOBAL_CONFIG' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::GLOBAL_CONFIG::Value get_GLOBAL_CONFIG() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_GLOBAL_CONFIG_raw() const = 0;

    // Read the register and slice out the 'cfg_en' field value.
    virtual bool get_GLOBAL_CONFIG_cfg_en() const = 0;

    // Read the register and slice out the 'cfg_brp_nomi' field value.
    virtual uint32_t get_GLOBAL_CONFIG_cfg_brp_nomi() const = 0;

    // Read the register and slice out the 'cfg_brp_data' field value.
    virtual uint32_t get_GLOBAL_CONFIG_cfg_brp_data() const = 0;

    // Read the register and slice out the 'cfg_td_en' field value.
    virtual bool get_GLOBAL_CONFIG_cfg_td_en() const = 0;

    // Read the register and slice out the 'cfg_ssp' field value.
    virtual uint32_t get_GLOBAL_CONFIG_cfg_ssp() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_GLOBAL_CONFIG(
      can_fd_fi::GLOBAL_CONFIG::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_GLOBAL_CONFIG_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'cfg_en' field value.
    // Will read-modify-write the register.
    virtual void set_GLOBAL_CONFIG_cfg_en(
      bool field_value
    ) const = 0;

    // Write the 'cfg_brp_nomi' field value.
    // Will read-modify-write the register.
    virtual void set_GLOBAL_CONFIG_cfg_brp_nomi(
      uint32_t field_value
    ) const = 0;

    // Write the 'cfg_brp_data' field value.
    // Will read-modify-write the register.
    virtual void set_GLOBAL_CONFIG_cfg_brp_data(
      uint32_t field_value
    ) const = 0;

    // Write the 'cfg_td_en' field value.
    // Will read-modify-write the register.
    virtual void set_GLOBAL_CONFIG_cfg_td_en(
      bool field_value
    ) const = 0;

    // Write the 'cfg_ssp' field value.
    // Will read-modify-write the register.
    virtual void set_GLOBAL_CONFIG_cfg_ssp(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'NOMINAL_CONFIG' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::NOMINAL_CONFIG::Value get_NOMINAL_CONFIG() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_NOMINAL_CONFIG_raw() const = 0;

    // Read the register and slice out the 'cfg_ph1_len_nomi' field value.
    virtual uint32_t get_NOMINAL_CONFIG_cfg_ph1_len_nomi() const = 0;

    // Read the register and slice out the 'cfg_ph2_len_nomi' field value.
    virtual uint32_t get_NOMINAL_CONFIG_cfg_ph2_len_nomi() const = 0;

    // Read the register and slice out the 'cfg_sjw_nomi' field value.
    virtual uint32_t get_NOMINAL_CONFIG_cfg_sjw_nomi() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_NOMINAL_CONFIG(
      can_fd_fi::NOMINAL_CONFIG::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_NOMINAL_CONFIG_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'cfg_ph1_len_nomi' field value.
    // Will read-modify-write the register.
    virtual void set_NOMINAL_CONFIG_cfg_ph1_len_nomi(
      uint32_t field_value
    ) const = 0;

    // Write the 'cfg_ph2_len_nomi' field value.
    // Will read-modify-write the register.
    virtual void set_NOMINAL_CONFIG_cfg_ph2_len_nomi(
      uint32_t field_value
    ) const = 0;

    // Write the 'cfg_sjw_nomi' field value.
    // Will read-modify-write the register.
    virtual void set_NOMINAL_CONFIG_cfg_sjw_nomi(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'DATA_CONFIG' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::DATA_CONFIG::Value get_DATA_CONFIG() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_DATA_CONFIG_raw() const = 0;

    // Read the register and slice out the 'cfg_ph1_len_data' field value.
    virtual uint32_t get_DATA_CONFIG_cfg_ph1_len_data() const = 0;

    // Read the register and slice out the 'cfg_ph2_len_data' field value.
    virtual uint32_t get_DATA_CONFIG_cfg_ph2_len_data() const = 0;

    // Read the register and slice out the 'cfg_sjw_data' field value.
    virtual uint32_t get_DATA_CONFIG_cfg_sjw_data() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_DATA_CONFIG(
      can_fd_fi::DATA_CONFIG::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_DATA_CONFIG_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'cfg_ph1_len_data' field value.
    // Will read-modify-write the register.
    virtual void set_DATA_CONFIG_cfg_ph1_len_data(
      uint32_t field_value
    ) const = 0;

    // Write the 'cfg_ph2_len_data' field value.
    // Will read-modify-write the register.
    virtual void set_DATA_CONFIG_cfg_ph2_len_data(
      uint32_t field_value
    ) const = 0;

    // Write the 'cfg_sjw_data' field value.
    // Will read-modify-write the register.
    virtual void set_DATA_CONFIG_cfg_sjw_data(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'FI_CONFIG' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::FI_CONFIG::Value get_FI_CONFIG() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_FI_CONFIG_raw() const = 0;

    // Read the register and slice out the 'cfg_psp_nomi' field value.
    virtual uint32_t get_FI_CONFIG_cfg_psp_nomi() const = 0;

    // Read the register and slice out the 'cfg_psp_data' field value.
    virtual uint32_t get_FI_CONFIG_cfg_psp_data() const = 0;

    // Read the register and slice out the 'cfg_fip_nomi' field value.
    virtual uint32_t get_FI_CONFIG_cfg_fip_nomi() const = 0;

    // Read the register and slice out the 'cfg_fip_data' field value.
    virtual uint32_t get_FI_CONFIG_cfg_fip_data() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_FI_CONFIG(
      can_fd_fi::FI_CONFIG::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_FI_CONFIG_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'cfg_psp_nomi' field value.
    // Will read-modify-write the register.
    virtual void set_FI_CONFIG_cfg_psp_nomi(
      uint32_t field_value
    ) const = 0;

    // Write the 'cfg_psp_data' field value.
    // Will read-modify-write the register.
    virtual void set_FI_CONFIG_cfg_psp_data(
      uint32_t field_value
    ) const = 0;

    // Write the 'cfg_fip_nomi' field value.
    // Will read-modify-write the register.
    virtual void set_FI_CONFIG_cfg_fip_nomi(
      uint32_t field_value
    ) const = 0;

    // Write the 'cfg_fip_data' field value.
    // Will read-modify-write the register.
    virtual void set_FI_CONFIG_cfg_fip_data(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'IRQ_RST_REG' register.
    // Mode 'Read'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::IRQ_RST_REG::Value get_IRQ_RST_REG() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_IRQ_RST_REG_raw() const = 0;

    // Read the register and slice out the 'irq_rst' field value.
    virtual uint32_t get_IRQ_RST_REG_irq_rst() const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM0_MATCH' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM0_MATCH::Value get_ID_MEM0_MATCH() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM0_MATCH_raw() const = 0;

    // Read the register and slice out the 'idm0_match' field value.
    virtual uint32_t get_ID_MEM0_MATCH_idm0_match() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM0_MATCH(
      can_fd_fi::ID_MEM0_MATCH::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM0_MATCH_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm0_match' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM0_MATCH_idm0_match(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM0_MASK' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM0_MASK::Value get_ID_MEM0_MASK() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM0_MASK_raw() const = 0;

    // Read the register and slice out the 'idm0_mask' field value.
    virtual uint32_t get_ID_MEM0_MASK_idm0_mask() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM0_MASK(
      can_fd_fi::ID_MEM0_MASK::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM0_MASK_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm0_mask' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM0_MASK_idm0_mask(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM0_FIF' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM0_FIF::Value get_ID_MEM0_FIF() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM0_FIF_raw() const = 0;

    // Read the register and slice out the 'idm0_fif' field value.
    virtual uint32_t get_ID_MEM0_FIF_idm0_fif() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM0_FIF(
      can_fd_fi::ID_MEM0_FIF::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM0_FIF_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm0_fif' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM0_FIF_idm0_fif(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM1_MATCH' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM1_MATCH::Value get_ID_MEM1_MATCH() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM1_MATCH_raw() const = 0;

    // Read the register and slice out the 'idm1_match' field value.
    virtual uint32_t get_ID_MEM1_MATCH_idm1_match() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM1_MATCH(
      can_fd_fi::ID_MEM1_MATCH::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM1_MATCH_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm1_match' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM1_MATCH_idm1_match(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM1_MASK' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM1_MASK::Value get_ID_MEM1_MASK() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM1_MASK_raw() const = 0;

    // Read the register and slice out the 'idm1_mask' field value.
    virtual uint32_t get_ID_MEM1_MASK_idm1_mask() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM1_MASK(
      can_fd_fi::ID_MEM1_MASK::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM1_MASK_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm1_mask' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM1_MASK_idm1_mask(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM1_FIF' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM1_FIF::Value get_ID_MEM1_FIF() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM1_FIF_raw() const = 0;

    // Read the register and slice out the 'idm1_fif' field value.
    virtual uint32_t get_ID_MEM1_FIF_idm1_fif() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM1_FIF(
      can_fd_fi::ID_MEM1_FIF::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM1_FIF_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm1_fif' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM1_FIF_idm1_fif(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM2_MATCH' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM2_MATCH::Value get_ID_MEM2_MATCH() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM2_MATCH_raw() const = 0;

    // Read the register and slice out the 'idm2_match' field value.
    virtual uint32_t get_ID_MEM2_MATCH_idm2_match() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM2_MATCH(
      can_fd_fi::ID_MEM2_MATCH::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM2_MATCH_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm2_match' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM2_MATCH_idm2_match(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM2_MASK' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM2_MASK::Value get_ID_MEM2_MASK() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM2_MASK_raw() const = 0;

    // Read the register and slice out the 'idm2_mask' field value.
    virtual uint32_t get_ID_MEM2_MASK_idm2_mask() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM2_MASK(
      can_fd_fi::ID_MEM2_MASK::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM2_MASK_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm2_mask' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM2_MASK_idm2_mask(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM2_FIF' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM2_FIF::Value get_ID_MEM2_FIF() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM2_FIF_raw() const = 0;

    // Read the register and slice out the 'idm2_fif' field value.
    virtual uint32_t get_ID_MEM2_FIF_idm2_fif() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM2_FIF(
      can_fd_fi::ID_MEM2_FIF::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM2_FIF_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm2_fif' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM2_FIF_idm2_fif(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM3_MATCH' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM3_MATCH::Value get_ID_MEM3_MATCH() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM3_MATCH_raw() const = 0;

    // Read the register and slice out the 'idm3_match' field value.
    virtual uint32_t get_ID_MEM3_MATCH_idm3_match() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM3_MATCH(
      can_fd_fi::ID_MEM3_MATCH::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM3_MATCH_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm3_match' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM3_MATCH_idm3_match(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM3_MASK' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM3_MASK::Value get_ID_MEM3_MASK() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM3_MASK_raw() const = 0;

    // Read the register and slice out the 'idm3_mask' field value.
    virtual uint32_t get_ID_MEM3_MASK_idm3_mask() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM3_MASK(
      can_fd_fi::ID_MEM3_MASK::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM3_MASK_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm3_mask' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM3_MASK_idm3_mask(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM3_FIF' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM3_FIF::Value get_ID_MEM3_FIF() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM3_FIF_raw() const = 0;

    // Read the register and slice out the 'idm3_fif' field value.
    virtual uint32_t get_ID_MEM3_FIF_idm3_fif() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM3_FIF(
      can_fd_fi::ID_MEM3_FIF::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM3_FIF_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm3_fif' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM3_FIF_idm3_fif(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM4_MATCH' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM4_MATCH::Value get_ID_MEM4_MATCH() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM4_MATCH_raw() const = 0;

    // Read the register and slice out the 'idm4_match' field value.
    virtual uint32_t get_ID_MEM4_MATCH_idm4_match() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM4_MATCH(
      can_fd_fi::ID_MEM4_MATCH::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM4_MATCH_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm4_match' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM4_MATCH_idm4_match(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM4_MASK' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM4_MASK::Value get_ID_MEM4_MASK() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM4_MASK_raw() const = 0;

    // Read the register and slice out the 'idm4_mask' field value.
    virtual uint32_t get_ID_MEM4_MASK_idm4_mask() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM4_MASK(
      can_fd_fi::ID_MEM4_MASK::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM4_MASK_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm4_mask' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM4_MASK_idm4_mask(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM4_FIF' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM4_FIF::Value get_ID_MEM4_FIF() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM4_FIF_raw() const = 0;

    // Read the register and slice out the 'idm4_fif' field value.
    virtual uint32_t get_ID_MEM4_FIF_idm4_fif() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM4_FIF(
      can_fd_fi::ID_MEM4_FIF::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM4_FIF_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm4_fif' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM4_FIF_idm4_fif(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM5_MATCH' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM5_MATCH::Value get_ID_MEM5_MATCH() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM5_MATCH_raw() const = 0;

    // Read the register and slice out the 'idm5_match' field value.
    virtual uint32_t get_ID_MEM5_MATCH_idm5_match() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM5_MATCH(
      can_fd_fi::ID_MEM5_MATCH::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM5_MATCH_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm5_match' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM5_MATCH_idm5_match(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM5_MASK' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM5_MASK::Value get_ID_MEM5_MASK() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM5_MASK_raw() const = 0;

    // Read the register and slice out the 'idm5_mask' field value.
    virtual uint32_t get_ID_MEM5_MASK_idm5_mask() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM5_MASK(
      can_fd_fi::ID_MEM5_MASK::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM5_MASK_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm5_mask' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM5_MASK_idm5_mask(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM5_FIF' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM5_FIF::Value get_ID_MEM5_FIF() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM5_FIF_raw() const = 0;

    // Read the register and slice out the 'idm5_fif' field value.
    virtual uint32_t get_ID_MEM5_FIF_idm5_fif() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM5_FIF(
      can_fd_fi::ID_MEM5_FIF::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM5_FIF_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm5_fif' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM5_FIF_idm5_fif(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM6_MATCH' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM6_MATCH::Value get_ID_MEM6_MATCH() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM6_MATCH_raw() const = 0;

    // Read the register and slice out the 'idm6_match' field value.
    virtual uint32_t get_ID_MEM6_MATCH_idm6_match() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM6_MATCH(
      can_fd_fi::ID_MEM6_MATCH::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM6_MATCH_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm6_match' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM6_MATCH_idm6_match(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM6_MASK' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM6_MASK::Value get_ID_MEM6_MASK() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM6_MASK_raw() const = 0;

    // Read the register and slice out the 'idm6_mask' field value.
    virtual uint32_t get_ID_MEM6_MASK_idm6_mask() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM6_MASK(
      can_fd_fi::ID_MEM6_MASK::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM6_MASK_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm6_mask' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM6_MASK_idm6_mask(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM6_FIF' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM6_FIF::Value get_ID_MEM6_FIF() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM6_FIF_raw() const = 0;

    // Read the register and slice out the 'idm6_fif' field value.
    virtual uint32_t get_ID_MEM6_FIF_idm6_fif() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM6_FIF(
      can_fd_fi::ID_MEM6_FIF::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM6_FIF_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm6_fif' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM6_FIF_idm6_fif(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM7_MATCH' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM7_MATCH::Value get_ID_MEM7_MATCH() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM7_MATCH_raw() const = 0;

    // Read the register and slice out the 'idm7_match' field value.
    virtual uint32_t get_ID_MEM7_MATCH_idm7_match() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM7_MATCH(
      can_fd_fi::ID_MEM7_MATCH::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM7_MATCH_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm7_match' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM7_MATCH_idm7_match(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM7_MASK' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM7_MASK::Value get_ID_MEM7_MASK() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM7_MASK_raw() const = 0;

    // Read the register and slice out the 'idm7_mask' field value.
    virtual uint32_t get_ID_MEM7_MASK_idm7_mask() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM7_MASK(
      can_fd_fi::ID_MEM7_MASK::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM7_MASK_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm7_mask' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM7_MASK_idm7_mask(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'ID_MEM7_FIF' register.
    // Mode 'Read, Write'.
    // -------------------------------------------------------------------------
    // Read the whole register value over the register bus.
    virtual can_fd_fi::ID_MEM7_FIF::Value get_ID_MEM7_FIF() const = 0;

    // Read the whole register value over the register bus.
    // This method returns the raw register value without any type conversion.
    virtual uint32_t get_ID_MEM7_FIF_raw() const = 0;

    // Read the register and slice out the 'idm7_fif' field value.
    virtual uint32_t get_ID_MEM7_FIF_idm7_fif() const = 0;

    // Write the whole register value over the register bus.
    virtual void set_ID_MEM7_FIF(
      can_fd_fi::ID_MEM7_FIF::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_ID_MEM7_FIF_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'idm7_fif' field value.
    // Will write the register with all other fields set as default.
    virtual void set_ID_MEM7_FIF_idm7_fif(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'MU_CFG_BASE' register.
    // Mode 'Write'.
    // -------------------------------------------------------------------------
    // Write the whole register value over the register bus.
    virtual void set_MU_CFG_BASE(
      can_fd_fi::MU_CFG_BASE::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_MU_CFG_BASE_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'ram_data_valid' field value.
    // Will write the register with all other fields set as default.
    virtual void set_MU_CFG_BASE_ram_data_valid(
      bool field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'MU_CFG_ADDR' register.
    // Mode 'Write'.
    // -------------------------------------------------------------------------
    // Write the whole register value over the register bus.
    virtual void set_MU_CFG_ADDR(
      can_fd_fi::MU_CFG_ADDR::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_MU_CFG_ADDR_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'ram_data_address' field value.
    // Will write the register with all other fields set as default.
    virtual void set_MU_CFG_ADDR_ram_data_address(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------

    // -------------------------------------------------------------------------
    // Methods for the 'MU_RAM_DATA' register.
    // Mode 'Write'.
    // -------------------------------------------------------------------------
    // Write the whole register value over the register bus.
    virtual void set_MU_RAM_DATA(
      can_fd_fi::MU_RAM_DATA::Value register_value
    ) const = 0;

    // Write the whole register value over the register bus.
    // This method takes a raw register value and does not perform any type conversion.
    virtual void set_MU_RAM_DATA_raw(
      uint32_t register_value
    ) const = 0;

    // Write the 'ram_data_payload' field value.
    // Will write the register with all other fields set as default.
    virtual void set_MU_RAM_DATA_ram_data_payload(
      uint32_t field_value
    ) const = 0;
    // -------------------------------------------------------------------------
  };

} // namespace fpga_regs
