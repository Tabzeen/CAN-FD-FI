// -----------------------------------------------------------------------------
// This file is automatically generated by hdl-registers version 8.1.0.
// Code generator CppImplementationGenerator version 2.0.2.
// -----------------------------------------------------------------------------

#include "include/can_fd_fi.h"

namespace fpga_regs
{

// Macros called by the register code below to check for runtime errors.
#ifdef NO_REGISTER_SETTER_ASSERT
  #define _SETTER_ASSERT_TRUE(expression, message) ((void)0)
#else
  #define _SETTER_ASSERT_TRUE(expression, message) (_ASSERT_TRUE(expression, message))
#endif

#ifdef NO_REGISTER_GETTER_ASSERT
  #define _GETTER_ASSERT_TRUE(expression, message) ((void)0)
#else
  #define _GETTER_ASSERT_TRUE(expression, message) (_ASSERT_TRUE(expression, message))
#endif

#ifdef NO_REGISTER_ARRAY_INDEX_ASSERT
  #define _ARRAY_INDEX_ASSERT_TRUE(expression, message) ((void)0)
#else
  #define _ARRAY_INDEX_ASSERT_TRUE(expression, message) (_ASSERT_TRUE(expression, message))
#endif

// Base macro called by the other macros.
#define _ASSERT_TRUE(expression, message)                                                      \
  {                                                                                            \
    if (!static_cast<bool>(expression)) {                                                      \
      std::ostringstream diagnostics;                                                          \
      diagnostics << "can_fd_fi.cpp:" << __LINE__ << ": " << message << ".";                   \
      std::string diagnostic_message = diagnostics.str();                                      \
      m_assertion_handler(&diagnostic_message);                                                \
    }                                                                                          \
  }

  CanFdFi::CanFdFi(uintptr_t base_address, bool (*assertion_handler) (const std::string*))
      : m_registers(reinterpret_cast<volatile uint32_t *>(base_address)),
        m_assertion_handler(assertion_handler)
  {
    // Empty
  }

  // ---------------------------------------------------------------------------
  // Methods for the 'CORE_INFO' register.
  // Mode 'Read'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::CORE_INFO::Value CanFdFi::get_CORE_INFO() const
  {
    const uint32_t raw_value = get_CORE_INFO_raw();

    const uint32_t info_t_value = get_CORE_INFO_info_t_from_raw(raw_value);

    return {info_t_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_CORE_INFO_raw() const
  {
    const size_t index = 0;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'info_t' field value.
  uint32_t CanFdFi::get_CORE_INFO_info_t() const
  {
    const uint32_t raw_value = get_CORE_INFO_raw();

    return get_CORE_INFO_info_t_from_raw(raw_value);
  }

  // Slice out the 'info_t' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_CORE_INFO_info_t_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::CORE_INFO::info_t::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::CORE_INFO::info_t::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'CORE_VERSION' register.
  // Mode 'Read'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::CORE_VERSION::Value CanFdFi::get_CORE_VERSION() const
  {
    const uint32_t raw_value = get_CORE_VERSION_raw();

    const uint32_t magic_valid_value = get_CORE_VERSION_magic_valid_from_raw(raw_value);
    const uint32_t patch_value = get_CORE_VERSION_patch_from_raw(raw_value);
    const uint32_t minor_value = get_CORE_VERSION_minor_from_raw(raw_value);
    const uint32_t major_value = get_CORE_VERSION_major_from_raw(raw_value);

    return {magic_valid_value, patch_value, minor_value, major_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_CORE_VERSION_raw() const
  {
    const size_t index = 1;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'magic_valid' field value.
  uint32_t CanFdFi::get_CORE_VERSION_magic_valid() const
  {
    const uint32_t raw_value = get_CORE_VERSION_raw();

    return get_CORE_VERSION_magic_valid_from_raw(raw_value);
  }

  // Slice out the 'magic_valid' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_CORE_VERSION_magic_valid_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::CORE_VERSION::magic_valid::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::CORE_VERSION::magic_valid::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Read the register and slice out the 'patch' field value.
  uint32_t CanFdFi::get_CORE_VERSION_patch() const
  {
    const uint32_t raw_value = get_CORE_VERSION_raw();

    return get_CORE_VERSION_patch_from_raw(raw_value);
  }

  // Slice out the 'patch' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_CORE_VERSION_patch_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::CORE_VERSION::patch::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::CORE_VERSION::patch::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Read the register and slice out the 'minor' field value.
  uint32_t CanFdFi::get_CORE_VERSION_minor() const
  {
    const uint32_t raw_value = get_CORE_VERSION_raw();

    return get_CORE_VERSION_minor_from_raw(raw_value);
  }

  // Slice out the 'minor' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_CORE_VERSION_minor_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::CORE_VERSION::minor::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::CORE_VERSION::minor::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Read the register and slice out the 'major' field value.
  uint32_t CanFdFi::get_CORE_VERSION_major() const
  {
    const uint32_t raw_value = get_CORE_VERSION_raw();

    return get_CORE_VERSION_major_from_raw(raw_value);
  }

  // Slice out the 'major' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_CORE_VERSION_major_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::CORE_VERSION::major::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::CORE_VERSION::major::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'STATUS_FLAGS' register.
  // Mode 'Read'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::STATUS_FLAGS::Value CanFdFi::get_STATUS_FLAGS() const
  {
    const uint32_t raw_value = get_STATUS_FLAGS_raw();

    const bool tu_ready_config_value = get_STATUS_FLAGS_tu_ready_config_from_raw(raw_value);
    const bool inj_err_miss_value = get_STATUS_FLAGS_inj_err_miss_from_raw(raw_value);
    const bool inj_err_dom_value = get_STATUS_FLAGS_inj_err_dom_from_raw(raw_value);
    const bool inj_err_dlc_value = get_STATUS_FLAGS_inj_err_dlc_from_raw(raw_value);
    const bool inj_err_fsm_value = get_STATUS_FLAGS_inj_err_fsm_from_raw(raw_value);

    return {tu_ready_config_value, inj_err_miss_value, inj_err_dom_value, inj_err_dlc_value, inj_err_fsm_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_STATUS_FLAGS_raw() const
  {
    const size_t index = 2;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'tu_ready_config' field value.
  bool CanFdFi::get_STATUS_FLAGS_tu_ready_config() const
  {
    const uint32_t raw_value = get_STATUS_FLAGS_raw();

    return get_STATUS_FLAGS_tu_ready_config_from_raw(raw_value);
  }

  // Slice out the 'tu_ready_config' field value from a given raw register value.
  // Performs no operation on the register bus.
  bool CanFdFi::get_STATUS_FLAGS_tu_ready_config_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::STATUS_FLAGS::tu_ready_config::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::STATUS_FLAGS::tu_ready_config::shift;

    // Convert to the result type.
    const bool field_value = static_cast<bool>(result_shifted);

    return field_value;
  }

  // Read the register and slice out the 'inj_err_miss' field value.
  bool CanFdFi::get_STATUS_FLAGS_inj_err_miss() const
  {
    const uint32_t raw_value = get_STATUS_FLAGS_raw();

    return get_STATUS_FLAGS_inj_err_miss_from_raw(raw_value);
  }

  // Slice out the 'inj_err_miss' field value from a given raw register value.
  // Performs no operation on the register bus.
  bool CanFdFi::get_STATUS_FLAGS_inj_err_miss_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::STATUS_FLAGS::inj_err_miss::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::STATUS_FLAGS::inj_err_miss::shift;

    // Convert to the result type.
    const bool field_value = static_cast<bool>(result_shifted);

    return field_value;
  }

  // Read the register and slice out the 'inj_err_dom' field value.
  bool CanFdFi::get_STATUS_FLAGS_inj_err_dom() const
  {
    const uint32_t raw_value = get_STATUS_FLAGS_raw();

    return get_STATUS_FLAGS_inj_err_dom_from_raw(raw_value);
  }

  // Slice out the 'inj_err_dom' field value from a given raw register value.
  // Performs no operation on the register bus.
  bool CanFdFi::get_STATUS_FLAGS_inj_err_dom_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::STATUS_FLAGS::inj_err_dom::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::STATUS_FLAGS::inj_err_dom::shift;

    // Convert to the result type.
    const bool field_value = static_cast<bool>(result_shifted);

    return field_value;
  }

  // Read the register and slice out the 'inj_err_dlc' field value.
  bool CanFdFi::get_STATUS_FLAGS_inj_err_dlc() const
  {
    const uint32_t raw_value = get_STATUS_FLAGS_raw();

    return get_STATUS_FLAGS_inj_err_dlc_from_raw(raw_value);
  }

  // Slice out the 'inj_err_dlc' field value from a given raw register value.
  // Performs no operation on the register bus.
  bool CanFdFi::get_STATUS_FLAGS_inj_err_dlc_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::STATUS_FLAGS::inj_err_dlc::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::STATUS_FLAGS::inj_err_dlc::shift;

    // Convert to the result type.
    const bool field_value = static_cast<bool>(result_shifted);

    return field_value;
  }

  // Read the register and slice out the 'inj_err_fsm' field value.
  bool CanFdFi::get_STATUS_FLAGS_inj_err_fsm() const
  {
    const uint32_t raw_value = get_STATUS_FLAGS_raw();

    return get_STATUS_FLAGS_inj_err_fsm_from_raw(raw_value);
  }

  // Slice out the 'inj_err_fsm' field value from a given raw register value.
  // Performs no operation on the register bus.
  bool CanFdFi::get_STATUS_FLAGS_inj_err_fsm_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::STATUS_FLAGS::inj_err_fsm::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::STATUS_FLAGS::inj_err_fsm::shift;

    // Convert to the result type.
    const bool field_value = static_cast<bool>(result_shifted);

    return field_value;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'GLOBAL_CONFIG' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::GLOBAL_CONFIG::Value CanFdFi::get_GLOBAL_CONFIG() const
  {
    const uint32_t raw_value = get_GLOBAL_CONFIG_raw();

    const bool cfg_en_value = get_GLOBAL_CONFIG_cfg_en_from_raw(raw_value);
    const uint32_t cfg_brp_nomi_value = get_GLOBAL_CONFIG_cfg_brp_nomi_from_raw(raw_value);
    const uint32_t cfg_brp_data_value = get_GLOBAL_CONFIG_cfg_brp_data_from_raw(raw_value);
    const bool cfg_td_en_value = get_GLOBAL_CONFIG_cfg_td_en_from_raw(raw_value);
    const uint32_t cfg_ssp_value = get_GLOBAL_CONFIG_cfg_ssp_from_raw(raw_value);

    return {cfg_en_value, cfg_brp_nomi_value, cfg_brp_data_value, cfg_td_en_value, cfg_ssp_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_GLOBAL_CONFIG_raw() const
  {
    const size_t index = 3;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'cfg_en' field value.
  bool CanFdFi::get_GLOBAL_CONFIG_cfg_en() const
  {
    const uint32_t raw_value = get_GLOBAL_CONFIG_raw();

    return get_GLOBAL_CONFIG_cfg_en_from_raw(raw_value);
  }

  // Slice out the 'cfg_en' field value from a given raw register value.
  // Performs no operation on the register bus.
  bool CanFdFi::get_GLOBAL_CONFIG_cfg_en_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::GLOBAL_CONFIG::cfg_en::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::GLOBAL_CONFIG::cfg_en::shift;

    // Convert to the result type.
    const bool field_value = static_cast<bool>(result_shifted);

    return field_value;
  }

  // Read the register and slice out the 'cfg_brp_nomi' field value.
  uint32_t CanFdFi::get_GLOBAL_CONFIG_cfg_brp_nomi() const
  {
    const uint32_t raw_value = get_GLOBAL_CONFIG_raw();

    return get_GLOBAL_CONFIG_cfg_brp_nomi_from_raw(raw_value);
  }

  // Slice out the 'cfg_brp_nomi' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_GLOBAL_CONFIG_cfg_brp_nomi_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::GLOBAL_CONFIG::cfg_brp_nomi::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::GLOBAL_CONFIG::cfg_brp_nomi::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    _GETTER_ASSERT_TRUE(
      field_value >= 1 && field_value <= 32,
      "Got 'cfg_brp_nomi' value out of range: " << field_value
    );

    return field_value;
  }

  // Read the register and slice out the 'cfg_brp_data' field value.
  uint32_t CanFdFi::get_GLOBAL_CONFIG_cfg_brp_data() const
  {
    const uint32_t raw_value = get_GLOBAL_CONFIG_raw();

    return get_GLOBAL_CONFIG_cfg_brp_data_from_raw(raw_value);
  }

  // Slice out the 'cfg_brp_data' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_GLOBAL_CONFIG_cfg_brp_data_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::GLOBAL_CONFIG::cfg_brp_data::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::GLOBAL_CONFIG::cfg_brp_data::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    _GETTER_ASSERT_TRUE(
      field_value >= 1 && field_value <= 32,
      "Got 'cfg_brp_data' value out of range: " << field_value
    );

    return field_value;
  }

  // Read the register and slice out the 'cfg_td_en' field value.
  bool CanFdFi::get_GLOBAL_CONFIG_cfg_td_en() const
  {
    const uint32_t raw_value = get_GLOBAL_CONFIG_raw();

    return get_GLOBAL_CONFIG_cfg_td_en_from_raw(raw_value);
  }

  // Slice out the 'cfg_td_en' field value from a given raw register value.
  // Performs no operation on the register bus.
  bool CanFdFi::get_GLOBAL_CONFIG_cfg_td_en_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::GLOBAL_CONFIG::cfg_td_en::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::GLOBAL_CONFIG::cfg_td_en::shift;

    // Convert to the result type.
    const bool field_value = static_cast<bool>(result_shifted);

    return field_value;
  }

  // Read the register and slice out the 'cfg_ssp' field value.
  uint32_t CanFdFi::get_GLOBAL_CONFIG_cfg_ssp() const
  {
    const uint32_t raw_value = get_GLOBAL_CONFIG_raw();

    return get_GLOBAL_CONFIG_cfg_ssp_from_raw(raw_value);
  }

  // Slice out the 'cfg_ssp' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_GLOBAL_CONFIG_cfg_ssp_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::GLOBAL_CONFIG::cfg_ssp::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::GLOBAL_CONFIG::cfg_ssp::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    _GETTER_ASSERT_TRUE(
      field_value <= 383,
      "Got 'cfg_ssp' value out of range: " << field_value
    );

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_GLOBAL_CONFIG(
    can_fd_fi::GLOBAL_CONFIG::Value register_value
  ) const
  {
    const uint32_t cfg_en_value = get_GLOBAL_CONFIG_cfg_en_to_raw(register_value.cfg_en);
    const uint32_t cfg_brp_nomi_value = get_GLOBAL_CONFIG_cfg_brp_nomi_to_raw(register_value.cfg_brp_nomi);
    const uint32_t cfg_brp_data_value = get_GLOBAL_CONFIG_cfg_brp_data_to_raw(register_value.cfg_brp_data);
    const uint32_t cfg_td_en_value = get_GLOBAL_CONFIG_cfg_td_en_to_raw(register_value.cfg_td_en);
    const uint32_t cfg_ssp_value = get_GLOBAL_CONFIG_cfg_ssp_to_raw(register_value.cfg_ssp);
    const uint32_t raw_value = cfg_en_value | cfg_brp_nomi_value | cfg_brp_data_value | cfg_td_en_value | cfg_ssp_value;

    set_GLOBAL_CONFIG_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_GLOBAL_CONFIG_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 3;
    m_registers[index] = register_value;
  }

  // Write the 'cfg_en' field value.
  // Will read-modify-write the register.
  void CanFdFi::set_GLOBAL_CONFIG_cfg_en(
    bool field_value
  ) const
  {
    const size_t index = 3;

    const uint32_t raw_value = m_registers[index];
    const uint32_t mask_shifted_inverse = ~can_fd_fi::GLOBAL_CONFIG::cfg_en::mask_shifted;
    const uint32_t base_value = raw_value & mask_shifted_inverse;

    const uint32_t field_value_raw = get_GLOBAL_CONFIG_cfg_en_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'cfg_en' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_GLOBAL_CONFIG_cfg_en_to_raw(bool field_value) const
  {
    const uint32_t field_value_casted = static_cast<uint32_t>(field_value);
    const uint32_t field_value_shifted = field_value_casted << can_fd_fi::GLOBAL_CONFIG::cfg_en::shift;

    return field_value_shifted;
  }

  // Write the 'cfg_brp_nomi' field value.
  // Will read-modify-write the register.
  void CanFdFi::set_GLOBAL_CONFIG_cfg_brp_nomi(
    uint32_t field_value
  ) const
  {
    const size_t index = 3;

    const uint32_t raw_value = m_registers[index];
    const uint32_t mask_shifted_inverse = ~can_fd_fi::GLOBAL_CONFIG::cfg_brp_nomi::mask_shifted;
    const uint32_t base_value = raw_value & mask_shifted_inverse;

    const uint32_t field_value_raw = get_GLOBAL_CONFIG_cfg_brp_nomi_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'cfg_brp_nomi' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_GLOBAL_CONFIG_cfg_brp_nomi_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 32,
      "Got 'cfg_brp_nomi' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::GLOBAL_CONFIG::cfg_brp_nomi::shift;

    return field_value_shifted;
  }

  // Write the 'cfg_brp_data' field value.
  // Will read-modify-write the register.
  void CanFdFi::set_GLOBAL_CONFIG_cfg_brp_data(
    uint32_t field_value
  ) const
  {
    const size_t index = 3;

    const uint32_t raw_value = m_registers[index];
    const uint32_t mask_shifted_inverse = ~can_fd_fi::GLOBAL_CONFIG::cfg_brp_data::mask_shifted;
    const uint32_t base_value = raw_value & mask_shifted_inverse;

    const uint32_t field_value_raw = get_GLOBAL_CONFIG_cfg_brp_data_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'cfg_brp_data' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_GLOBAL_CONFIG_cfg_brp_data_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 32,
      "Got 'cfg_brp_data' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::GLOBAL_CONFIG::cfg_brp_data::shift;

    return field_value_shifted;
  }

  // Write the 'cfg_td_en' field value.
  // Will read-modify-write the register.
  void CanFdFi::set_GLOBAL_CONFIG_cfg_td_en(
    bool field_value
  ) const
  {
    const size_t index = 3;

    const uint32_t raw_value = m_registers[index];
    const uint32_t mask_shifted_inverse = ~can_fd_fi::GLOBAL_CONFIG::cfg_td_en::mask_shifted;
    const uint32_t base_value = raw_value & mask_shifted_inverse;

    const uint32_t field_value_raw = get_GLOBAL_CONFIG_cfg_td_en_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'cfg_td_en' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_GLOBAL_CONFIG_cfg_td_en_to_raw(bool field_value) const
  {
    const uint32_t field_value_casted = static_cast<uint32_t>(field_value);
    const uint32_t field_value_shifted = field_value_casted << can_fd_fi::GLOBAL_CONFIG::cfg_td_en::shift;

    return field_value_shifted;
  }

  // Write the 'cfg_ssp' field value.
  // Will read-modify-write the register.
  void CanFdFi::set_GLOBAL_CONFIG_cfg_ssp(
    uint32_t field_value
  ) const
  {
    const size_t index = 3;

    const uint32_t raw_value = m_registers[index];
    const uint32_t mask_shifted_inverse = ~can_fd_fi::GLOBAL_CONFIG::cfg_ssp::mask_shifted;
    const uint32_t base_value = raw_value & mask_shifted_inverse;

    const uint32_t field_value_raw = get_GLOBAL_CONFIG_cfg_ssp_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'cfg_ssp' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_GLOBAL_CONFIG_cfg_ssp_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 383,
      "Got 'cfg_ssp' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::GLOBAL_CONFIG::cfg_ssp::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'NOMINAL_CONFIG' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::NOMINAL_CONFIG::Value CanFdFi::get_NOMINAL_CONFIG() const
  {
    const uint32_t raw_value = get_NOMINAL_CONFIG_raw();

    const uint32_t cfg_ph1_len_nomi_value = get_NOMINAL_CONFIG_cfg_ph1_len_nomi_from_raw(raw_value);
    const uint32_t cfg_ph2_len_nomi_value = get_NOMINAL_CONFIG_cfg_ph2_len_nomi_from_raw(raw_value);
    const uint32_t cfg_sjw_nomi_value = get_NOMINAL_CONFIG_cfg_sjw_nomi_from_raw(raw_value);

    return {cfg_ph1_len_nomi_value, cfg_ph2_len_nomi_value, cfg_sjw_nomi_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_NOMINAL_CONFIG_raw() const
  {
    const size_t index = 4;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'cfg_ph1_len_nomi' field value.
  uint32_t CanFdFi::get_NOMINAL_CONFIG_cfg_ph1_len_nomi() const
  {
    const uint32_t raw_value = get_NOMINAL_CONFIG_raw();

    return get_NOMINAL_CONFIG_cfg_ph1_len_nomi_from_raw(raw_value);
  }

  // Slice out the 'cfg_ph1_len_nomi' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_NOMINAL_CONFIG_cfg_ph1_len_nomi_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::NOMINAL_CONFIG::cfg_ph1_len_nomi::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::NOMINAL_CONFIG::cfg_ph1_len_nomi::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    _GETTER_ASSERT_TRUE(
      field_value >= 1,
      "Got 'cfg_ph1_len_nomi' value out of range: " << field_value
    );

    return field_value;
  }

  // Read the register and slice out the 'cfg_ph2_len_nomi' field value.
  uint32_t CanFdFi::get_NOMINAL_CONFIG_cfg_ph2_len_nomi() const
  {
    const uint32_t raw_value = get_NOMINAL_CONFIG_raw();

    return get_NOMINAL_CONFIG_cfg_ph2_len_nomi_from_raw(raw_value);
  }

  // Slice out the 'cfg_ph2_len_nomi' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_NOMINAL_CONFIG_cfg_ph2_len_nomi_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::NOMINAL_CONFIG::cfg_ph2_len_nomi::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::NOMINAL_CONFIG::cfg_ph2_len_nomi::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    _GETTER_ASSERT_TRUE(
      field_value >= 1,
      "Got 'cfg_ph2_len_nomi' value out of range: " << field_value
    );

    return field_value;
  }

  // Read the register and slice out the 'cfg_sjw_nomi' field value.
  uint32_t CanFdFi::get_NOMINAL_CONFIG_cfg_sjw_nomi() const
  {
    const uint32_t raw_value = get_NOMINAL_CONFIG_raw();

    return get_NOMINAL_CONFIG_cfg_sjw_nomi_from_raw(raw_value);
  }

  // Slice out the 'cfg_sjw_nomi' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_NOMINAL_CONFIG_cfg_sjw_nomi_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::NOMINAL_CONFIG::cfg_sjw_nomi::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::NOMINAL_CONFIG::cfg_sjw_nomi::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    _GETTER_ASSERT_TRUE(
      field_value >= 1,
      "Got 'cfg_sjw_nomi' value out of range: " << field_value
    );

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_NOMINAL_CONFIG(
    can_fd_fi::NOMINAL_CONFIG::Value register_value
  ) const
  {
    const uint32_t cfg_ph1_len_nomi_value = get_NOMINAL_CONFIG_cfg_ph1_len_nomi_to_raw(register_value.cfg_ph1_len_nomi);
    const uint32_t cfg_ph2_len_nomi_value = get_NOMINAL_CONFIG_cfg_ph2_len_nomi_to_raw(register_value.cfg_ph2_len_nomi);
    const uint32_t cfg_sjw_nomi_value = get_NOMINAL_CONFIG_cfg_sjw_nomi_to_raw(register_value.cfg_sjw_nomi);
    const uint32_t raw_value = cfg_ph1_len_nomi_value | cfg_ph2_len_nomi_value | cfg_sjw_nomi_value;

    set_NOMINAL_CONFIG_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_NOMINAL_CONFIG_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 4;
    m_registers[index] = register_value;
  }

  // Write the 'cfg_ph1_len_nomi' field value.
  // Will read-modify-write the register.
  void CanFdFi::set_NOMINAL_CONFIG_cfg_ph1_len_nomi(
    uint32_t field_value
  ) const
  {
    const size_t index = 4;

    const uint32_t raw_value = m_registers[index];
    const uint32_t mask_shifted_inverse = ~can_fd_fi::NOMINAL_CONFIG::cfg_ph1_len_nomi::mask_shifted;
    const uint32_t base_value = raw_value & mask_shifted_inverse;

    const uint32_t field_value_raw = get_NOMINAL_CONFIG_cfg_ph1_len_nomi_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'cfg_ph1_len_nomi' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_NOMINAL_CONFIG_cfg_ph1_len_nomi_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 255,
      "Got 'cfg_ph1_len_nomi' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::NOMINAL_CONFIG::cfg_ph1_len_nomi::shift;

    return field_value_shifted;
  }

  // Write the 'cfg_ph2_len_nomi' field value.
  // Will read-modify-write the register.
  void CanFdFi::set_NOMINAL_CONFIG_cfg_ph2_len_nomi(
    uint32_t field_value
  ) const
  {
    const size_t index = 4;

    const uint32_t raw_value = m_registers[index];
    const uint32_t mask_shifted_inverse = ~can_fd_fi::NOMINAL_CONFIG::cfg_ph2_len_nomi::mask_shifted;
    const uint32_t base_value = raw_value & mask_shifted_inverse;

    const uint32_t field_value_raw = get_NOMINAL_CONFIG_cfg_ph2_len_nomi_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'cfg_ph2_len_nomi' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_NOMINAL_CONFIG_cfg_ph2_len_nomi_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 127,
      "Got 'cfg_ph2_len_nomi' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::NOMINAL_CONFIG::cfg_ph2_len_nomi::shift;

    return field_value_shifted;
  }

  // Write the 'cfg_sjw_nomi' field value.
  // Will read-modify-write the register.
  void CanFdFi::set_NOMINAL_CONFIG_cfg_sjw_nomi(
    uint32_t field_value
  ) const
  {
    const size_t index = 4;

    const uint32_t raw_value = m_registers[index];
    const uint32_t mask_shifted_inverse = ~can_fd_fi::NOMINAL_CONFIG::cfg_sjw_nomi::mask_shifted;
    const uint32_t base_value = raw_value & mask_shifted_inverse;

    const uint32_t field_value_raw = get_NOMINAL_CONFIG_cfg_sjw_nomi_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'cfg_sjw_nomi' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_NOMINAL_CONFIG_cfg_sjw_nomi_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 127,
      "Got 'cfg_sjw_nomi' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::NOMINAL_CONFIG::cfg_sjw_nomi::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'DATA_CONFIG' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::DATA_CONFIG::Value CanFdFi::get_DATA_CONFIG() const
  {
    const uint32_t raw_value = get_DATA_CONFIG_raw();

    const uint32_t cfg_ph1_len_data_value = get_DATA_CONFIG_cfg_ph1_len_data_from_raw(raw_value);
    const uint32_t cfg_ph2_len_data_value = get_DATA_CONFIG_cfg_ph2_len_data_from_raw(raw_value);
    const uint32_t cfg_sjw_data_value = get_DATA_CONFIG_cfg_sjw_data_from_raw(raw_value);

    return {cfg_ph1_len_data_value, cfg_ph2_len_data_value, cfg_sjw_data_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_DATA_CONFIG_raw() const
  {
    const size_t index = 5;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'cfg_ph1_len_data' field value.
  uint32_t CanFdFi::get_DATA_CONFIG_cfg_ph1_len_data() const
  {
    const uint32_t raw_value = get_DATA_CONFIG_raw();

    return get_DATA_CONFIG_cfg_ph1_len_data_from_raw(raw_value);
  }

  // Slice out the 'cfg_ph1_len_data' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_DATA_CONFIG_cfg_ph1_len_data_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::DATA_CONFIG::cfg_ph1_len_data::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::DATA_CONFIG::cfg_ph1_len_data::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    _GETTER_ASSERT_TRUE(
      field_value >= 1,
      "Got 'cfg_ph1_len_data' value out of range: " << field_value
    );

    return field_value;
  }

  // Read the register and slice out the 'cfg_ph2_len_data' field value.
  uint32_t CanFdFi::get_DATA_CONFIG_cfg_ph2_len_data() const
  {
    const uint32_t raw_value = get_DATA_CONFIG_raw();

    return get_DATA_CONFIG_cfg_ph2_len_data_from_raw(raw_value);
  }

  // Slice out the 'cfg_ph2_len_data' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_DATA_CONFIG_cfg_ph2_len_data_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::DATA_CONFIG::cfg_ph2_len_data::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::DATA_CONFIG::cfg_ph2_len_data::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    _GETTER_ASSERT_TRUE(
      field_value >= 1,
      "Got 'cfg_ph2_len_data' value out of range: " << field_value
    );

    return field_value;
  }

  // Read the register and slice out the 'cfg_sjw_data' field value.
  uint32_t CanFdFi::get_DATA_CONFIG_cfg_sjw_data() const
  {
    const uint32_t raw_value = get_DATA_CONFIG_raw();

    return get_DATA_CONFIG_cfg_sjw_data_from_raw(raw_value);
  }

  // Slice out the 'cfg_sjw_data' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_DATA_CONFIG_cfg_sjw_data_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::DATA_CONFIG::cfg_sjw_data::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::DATA_CONFIG::cfg_sjw_data::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    _GETTER_ASSERT_TRUE(
      field_value >= 1,
      "Got 'cfg_sjw_data' value out of range: " << field_value
    );

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_DATA_CONFIG(
    can_fd_fi::DATA_CONFIG::Value register_value
  ) const
  {
    const uint32_t cfg_ph1_len_data_value = get_DATA_CONFIG_cfg_ph1_len_data_to_raw(register_value.cfg_ph1_len_data);
    const uint32_t cfg_ph2_len_data_value = get_DATA_CONFIG_cfg_ph2_len_data_to_raw(register_value.cfg_ph2_len_data);
    const uint32_t cfg_sjw_data_value = get_DATA_CONFIG_cfg_sjw_data_to_raw(register_value.cfg_sjw_data);
    const uint32_t raw_value = cfg_ph1_len_data_value | cfg_ph2_len_data_value | cfg_sjw_data_value;

    set_DATA_CONFIG_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_DATA_CONFIG_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 5;
    m_registers[index] = register_value;
  }

  // Write the 'cfg_ph1_len_data' field value.
  // Will read-modify-write the register.
  void CanFdFi::set_DATA_CONFIG_cfg_ph1_len_data(
    uint32_t field_value
  ) const
  {
    const size_t index = 5;

    const uint32_t raw_value = m_registers[index];
    const uint32_t mask_shifted_inverse = ~can_fd_fi::DATA_CONFIG::cfg_ph1_len_data::mask_shifted;
    const uint32_t base_value = raw_value & mask_shifted_inverse;

    const uint32_t field_value_raw = get_DATA_CONFIG_cfg_ph1_len_data_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'cfg_ph1_len_data' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_DATA_CONFIG_cfg_ph1_len_data_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 255,
      "Got 'cfg_ph1_len_data' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::DATA_CONFIG::cfg_ph1_len_data::shift;

    return field_value_shifted;
  }

  // Write the 'cfg_ph2_len_data' field value.
  // Will read-modify-write the register.
  void CanFdFi::set_DATA_CONFIG_cfg_ph2_len_data(
    uint32_t field_value
  ) const
  {
    const size_t index = 5;

    const uint32_t raw_value = m_registers[index];
    const uint32_t mask_shifted_inverse = ~can_fd_fi::DATA_CONFIG::cfg_ph2_len_data::mask_shifted;
    const uint32_t base_value = raw_value & mask_shifted_inverse;

    const uint32_t field_value_raw = get_DATA_CONFIG_cfg_ph2_len_data_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'cfg_ph2_len_data' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_DATA_CONFIG_cfg_ph2_len_data_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 127,
      "Got 'cfg_ph2_len_data' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::DATA_CONFIG::cfg_ph2_len_data::shift;

    return field_value_shifted;
  }

  // Write the 'cfg_sjw_data' field value.
  // Will read-modify-write the register.
  void CanFdFi::set_DATA_CONFIG_cfg_sjw_data(
    uint32_t field_value
  ) const
  {
    const size_t index = 5;

    const uint32_t raw_value = m_registers[index];
    const uint32_t mask_shifted_inverse = ~can_fd_fi::DATA_CONFIG::cfg_sjw_data::mask_shifted;
    const uint32_t base_value = raw_value & mask_shifted_inverse;

    const uint32_t field_value_raw = get_DATA_CONFIG_cfg_sjw_data_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'cfg_sjw_data' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_DATA_CONFIG_cfg_sjw_data_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 127,
      "Got 'cfg_sjw_data' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::DATA_CONFIG::cfg_sjw_data::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'FI_CONFIG' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::FI_CONFIG::Value CanFdFi::get_FI_CONFIG() const
  {
    const uint32_t raw_value = get_FI_CONFIG_raw();

    const uint32_t cfg_psp_nomi_value = get_FI_CONFIG_cfg_psp_nomi_from_raw(raw_value);
    const uint32_t cfg_psp_data_value = get_FI_CONFIG_cfg_psp_data_from_raw(raw_value);
    const uint32_t cfg_fip_nomi_value = get_FI_CONFIG_cfg_fip_nomi_from_raw(raw_value);
    const uint32_t cfg_fip_data_value = get_FI_CONFIG_cfg_fip_data_from_raw(raw_value);

    return {cfg_psp_nomi_value, cfg_psp_data_value, cfg_fip_nomi_value, cfg_fip_data_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_FI_CONFIG_raw() const
  {
    const size_t index = 6;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'cfg_psp_nomi' field value.
  uint32_t CanFdFi::get_FI_CONFIG_cfg_psp_nomi() const
  {
    const uint32_t raw_value = get_FI_CONFIG_raw();

    return get_FI_CONFIG_cfg_psp_nomi_from_raw(raw_value);
  }

  // Slice out the 'cfg_psp_nomi' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_FI_CONFIG_cfg_psp_nomi_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::FI_CONFIG::cfg_psp_nomi::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::FI_CONFIG::cfg_psp_nomi::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Read the register and slice out the 'cfg_psp_data' field value.
  uint32_t CanFdFi::get_FI_CONFIG_cfg_psp_data() const
  {
    const uint32_t raw_value = get_FI_CONFIG_raw();

    return get_FI_CONFIG_cfg_psp_data_from_raw(raw_value);
  }

  // Slice out the 'cfg_psp_data' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_FI_CONFIG_cfg_psp_data_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::FI_CONFIG::cfg_psp_data::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::FI_CONFIG::cfg_psp_data::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Read the register and slice out the 'cfg_fip_nomi' field value.
  uint32_t CanFdFi::get_FI_CONFIG_cfg_fip_nomi() const
  {
    const uint32_t raw_value = get_FI_CONFIG_raw();

    return get_FI_CONFIG_cfg_fip_nomi_from_raw(raw_value);
  }

  // Slice out the 'cfg_fip_nomi' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_FI_CONFIG_cfg_fip_nomi_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::FI_CONFIG::cfg_fip_nomi::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::FI_CONFIG::cfg_fip_nomi::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Read the register and slice out the 'cfg_fip_data' field value.
  uint32_t CanFdFi::get_FI_CONFIG_cfg_fip_data() const
  {
    const uint32_t raw_value = get_FI_CONFIG_raw();

    return get_FI_CONFIG_cfg_fip_data_from_raw(raw_value);
  }

  // Slice out the 'cfg_fip_data' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_FI_CONFIG_cfg_fip_data_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::FI_CONFIG::cfg_fip_data::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::FI_CONFIG::cfg_fip_data::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_FI_CONFIG(
    can_fd_fi::FI_CONFIG::Value register_value
  ) const
  {
    const uint32_t cfg_psp_nomi_value = get_FI_CONFIG_cfg_psp_nomi_to_raw(register_value.cfg_psp_nomi);
    const uint32_t cfg_psp_data_value = get_FI_CONFIG_cfg_psp_data_to_raw(register_value.cfg_psp_data);
    const uint32_t cfg_fip_nomi_value = get_FI_CONFIG_cfg_fip_nomi_to_raw(register_value.cfg_fip_nomi);
    const uint32_t cfg_fip_data_value = get_FI_CONFIG_cfg_fip_data_to_raw(register_value.cfg_fip_data);
    const uint32_t raw_value = cfg_psp_nomi_value | cfg_psp_data_value | cfg_fip_nomi_value | cfg_fip_data_value;

    set_FI_CONFIG_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_FI_CONFIG_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 6;
    m_registers[index] = register_value;
  }

  // Write the 'cfg_psp_nomi' field value.
  // Will read-modify-write the register.
  void CanFdFi::set_FI_CONFIG_cfg_psp_nomi(
    uint32_t field_value
  ) const
  {
    const size_t index = 6;

    const uint32_t raw_value = m_registers[index];
    const uint32_t mask_shifted_inverse = ~can_fd_fi::FI_CONFIG::cfg_psp_nomi::mask_shifted;
    const uint32_t base_value = raw_value & mask_shifted_inverse;

    const uint32_t field_value_raw = get_FI_CONFIG_cfg_psp_nomi_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'cfg_psp_nomi' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_FI_CONFIG_cfg_psp_nomi_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 255,
      "Got 'cfg_psp_nomi' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::FI_CONFIG::cfg_psp_nomi::shift;

    return field_value_shifted;
  }

  // Write the 'cfg_psp_data' field value.
  // Will read-modify-write the register.
  void CanFdFi::set_FI_CONFIG_cfg_psp_data(
    uint32_t field_value
  ) const
  {
    const size_t index = 6;

    const uint32_t raw_value = m_registers[index];
    const uint32_t mask_shifted_inverse = ~can_fd_fi::FI_CONFIG::cfg_psp_data::mask_shifted;
    const uint32_t base_value = raw_value & mask_shifted_inverse;

    const uint32_t field_value_raw = get_FI_CONFIG_cfg_psp_data_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'cfg_psp_data' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_FI_CONFIG_cfg_psp_data_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 255,
      "Got 'cfg_psp_data' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::FI_CONFIG::cfg_psp_data::shift;

    return field_value_shifted;
  }

  // Write the 'cfg_fip_nomi' field value.
  // Will read-modify-write the register.
  void CanFdFi::set_FI_CONFIG_cfg_fip_nomi(
    uint32_t field_value
  ) const
  {
    const size_t index = 6;

    const uint32_t raw_value = m_registers[index];
    const uint32_t mask_shifted_inverse = ~can_fd_fi::FI_CONFIG::cfg_fip_nomi::mask_shifted;
    const uint32_t base_value = raw_value & mask_shifted_inverse;

    const uint32_t field_value_raw = get_FI_CONFIG_cfg_fip_nomi_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'cfg_fip_nomi' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_FI_CONFIG_cfg_fip_nomi_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 255,
      "Got 'cfg_fip_nomi' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::FI_CONFIG::cfg_fip_nomi::shift;

    return field_value_shifted;
  }

  // Write the 'cfg_fip_data' field value.
  // Will read-modify-write the register.
  void CanFdFi::set_FI_CONFIG_cfg_fip_data(
    uint32_t field_value
  ) const
  {
    const size_t index = 6;

    const uint32_t raw_value = m_registers[index];
    const uint32_t mask_shifted_inverse = ~can_fd_fi::FI_CONFIG::cfg_fip_data::mask_shifted;
    const uint32_t base_value = raw_value & mask_shifted_inverse;

    const uint32_t field_value_raw = get_FI_CONFIG_cfg_fip_data_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'cfg_fip_data' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_FI_CONFIG_cfg_fip_data_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 255,
      "Got 'cfg_fip_data' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::FI_CONFIG::cfg_fip_data::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'IRQ_RST_REG' register.
  // Mode 'Read'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::IRQ_RST_REG::Value CanFdFi::get_IRQ_RST_REG() const
  {
    const uint32_t raw_value = get_IRQ_RST_REG_raw();

    const uint32_t irq_rst_value = get_IRQ_RST_REG_irq_rst_from_raw(raw_value);

    return {irq_rst_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_IRQ_RST_REG_raw() const
  {
    const size_t index = 7;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'irq_rst' field value.
  uint32_t CanFdFi::get_IRQ_RST_REG_irq_rst() const
  {
    const uint32_t raw_value = get_IRQ_RST_REG_raw();

    return get_IRQ_RST_REG_irq_rst_from_raw(raw_value);
  }

  // Slice out the 'irq_rst' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_IRQ_RST_REG_irq_rst_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::IRQ_RST_REG::irq_rst::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::IRQ_RST_REG::irq_rst::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM0_MATCH' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM0_MATCH::Value CanFdFi::get_ID_MEM0_MATCH() const
  {
    const uint32_t raw_value = get_ID_MEM0_MATCH_raw();

    const uint32_t idm0_match_value = get_ID_MEM0_MATCH_idm0_match_from_raw(raw_value);

    return {idm0_match_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM0_MATCH_raw() const
  {
    const size_t index = 8;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm0_match' field value.
  uint32_t CanFdFi::get_ID_MEM0_MATCH_idm0_match() const
  {
    const uint32_t raw_value = get_ID_MEM0_MATCH_raw();

    return get_ID_MEM0_MATCH_idm0_match_from_raw(raw_value);
  }

  // Slice out the 'idm0_match' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM0_MATCH_idm0_match_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM0_MATCH::idm0_match::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM0_MATCH::idm0_match::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM0_MATCH(
    can_fd_fi::ID_MEM0_MATCH::Value register_value
  ) const
  {
    const uint32_t idm0_match_value = get_ID_MEM0_MATCH_idm0_match_to_raw(register_value.idm0_match);
    const uint32_t raw_value = idm0_match_value;

    set_ID_MEM0_MATCH_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM0_MATCH_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 8;
    m_registers[index] = register_value;
  }

  // Write the 'idm0_match' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM0_MATCH_idm0_match(
    uint32_t field_value
  ) const
  {
    const size_t index = 8;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM0_MATCH_idm0_match_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm0_match' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM0_MATCH_idm0_match_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 536870911,
      "Got 'idm0_match' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM0_MATCH::idm0_match::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM0_MASK' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM0_MASK::Value CanFdFi::get_ID_MEM0_MASK() const
  {
    const uint32_t raw_value = get_ID_MEM0_MASK_raw();

    const uint32_t idm0_mask_value = get_ID_MEM0_MASK_idm0_mask_from_raw(raw_value);

    return {idm0_mask_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM0_MASK_raw() const
  {
    const size_t index = 9;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm0_mask' field value.
  uint32_t CanFdFi::get_ID_MEM0_MASK_idm0_mask() const
  {
    const uint32_t raw_value = get_ID_MEM0_MASK_raw();

    return get_ID_MEM0_MASK_idm0_mask_from_raw(raw_value);
  }

  // Slice out the 'idm0_mask' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM0_MASK_idm0_mask_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM0_MASK::idm0_mask::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM0_MASK::idm0_mask::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM0_MASK(
    can_fd_fi::ID_MEM0_MASK::Value register_value
  ) const
  {
    const uint32_t idm0_mask_value = get_ID_MEM0_MASK_idm0_mask_to_raw(register_value.idm0_mask);
    const uint32_t raw_value = idm0_mask_value;

    set_ID_MEM0_MASK_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM0_MASK_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 9;
    m_registers[index] = register_value;
  }

  // Write the 'idm0_mask' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM0_MASK_idm0_mask(
    uint32_t field_value
  ) const
  {
    const size_t index = 9;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM0_MASK_idm0_mask_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm0_mask' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM0_MASK_idm0_mask_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 536870911,
      "Got 'idm0_mask' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM0_MASK::idm0_mask::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM0_FIF' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM0_FIF::Value CanFdFi::get_ID_MEM0_FIF() const
  {
    const uint32_t raw_value = get_ID_MEM0_FIF_raw();

    const uint32_t idm0_fif_value = get_ID_MEM0_FIF_idm0_fif_from_raw(raw_value);

    return {idm0_fif_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM0_FIF_raw() const
  {
    const size_t index = 10;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm0_fif' field value.
  uint32_t CanFdFi::get_ID_MEM0_FIF_idm0_fif() const
  {
    const uint32_t raw_value = get_ID_MEM0_FIF_raw();

    return get_ID_MEM0_FIF_idm0_fif_from_raw(raw_value);
  }

  // Slice out the 'idm0_fif' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM0_FIF_idm0_fif_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM0_FIF::idm0_fif::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM0_FIF::idm0_fif::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM0_FIF(
    can_fd_fi::ID_MEM0_FIF::Value register_value
  ) const
  {
    const uint32_t idm0_fif_value = get_ID_MEM0_FIF_idm0_fif_to_raw(register_value.idm0_fif);
    const uint32_t raw_value = idm0_fif_value;

    set_ID_MEM0_FIF_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM0_FIF_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 10;
    m_registers[index] = register_value;
  }

  // Write the 'idm0_fif' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM0_FIF_idm0_fif(
    uint32_t field_value
  ) const
  {
    const size_t index = 10;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM0_FIF_idm0_fif_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm0_fif' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM0_FIF_idm0_fif_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 262143,
      "Got 'idm0_fif' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM0_FIF::idm0_fif::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM1_MATCH' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM1_MATCH::Value CanFdFi::get_ID_MEM1_MATCH() const
  {
    const uint32_t raw_value = get_ID_MEM1_MATCH_raw();

    const uint32_t idm1_match_value = get_ID_MEM1_MATCH_idm1_match_from_raw(raw_value);

    return {idm1_match_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM1_MATCH_raw() const
  {
    const size_t index = 11;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm1_match' field value.
  uint32_t CanFdFi::get_ID_MEM1_MATCH_idm1_match() const
  {
    const uint32_t raw_value = get_ID_MEM1_MATCH_raw();

    return get_ID_MEM1_MATCH_idm1_match_from_raw(raw_value);
  }

  // Slice out the 'idm1_match' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM1_MATCH_idm1_match_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM1_MATCH::idm1_match::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM1_MATCH::idm1_match::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM1_MATCH(
    can_fd_fi::ID_MEM1_MATCH::Value register_value
  ) const
  {
    const uint32_t idm1_match_value = get_ID_MEM1_MATCH_idm1_match_to_raw(register_value.idm1_match);
    const uint32_t raw_value = idm1_match_value;

    set_ID_MEM1_MATCH_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM1_MATCH_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 11;
    m_registers[index] = register_value;
  }

  // Write the 'idm1_match' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM1_MATCH_idm1_match(
    uint32_t field_value
  ) const
  {
    const size_t index = 11;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM1_MATCH_idm1_match_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm1_match' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM1_MATCH_idm1_match_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 536870911,
      "Got 'idm1_match' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM1_MATCH::idm1_match::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM1_MASK' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM1_MASK::Value CanFdFi::get_ID_MEM1_MASK() const
  {
    const uint32_t raw_value = get_ID_MEM1_MASK_raw();

    const uint32_t idm1_mask_value = get_ID_MEM1_MASK_idm1_mask_from_raw(raw_value);

    return {idm1_mask_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM1_MASK_raw() const
  {
    const size_t index = 12;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm1_mask' field value.
  uint32_t CanFdFi::get_ID_MEM1_MASK_idm1_mask() const
  {
    const uint32_t raw_value = get_ID_MEM1_MASK_raw();

    return get_ID_MEM1_MASK_idm1_mask_from_raw(raw_value);
  }

  // Slice out the 'idm1_mask' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM1_MASK_idm1_mask_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM1_MASK::idm1_mask::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM1_MASK::idm1_mask::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM1_MASK(
    can_fd_fi::ID_MEM1_MASK::Value register_value
  ) const
  {
    const uint32_t idm1_mask_value = get_ID_MEM1_MASK_idm1_mask_to_raw(register_value.idm1_mask);
    const uint32_t raw_value = idm1_mask_value;

    set_ID_MEM1_MASK_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM1_MASK_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 12;
    m_registers[index] = register_value;
  }

  // Write the 'idm1_mask' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM1_MASK_idm1_mask(
    uint32_t field_value
  ) const
  {
    const size_t index = 12;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM1_MASK_idm1_mask_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm1_mask' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM1_MASK_idm1_mask_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 536870911,
      "Got 'idm1_mask' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM1_MASK::idm1_mask::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM1_FIF' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM1_FIF::Value CanFdFi::get_ID_MEM1_FIF() const
  {
    const uint32_t raw_value = get_ID_MEM1_FIF_raw();

    const uint32_t idm1_fif_value = get_ID_MEM1_FIF_idm1_fif_from_raw(raw_value);

    return {idm1_fif_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM1_FIF_raw() const
  {
    const size_t index = 13;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm1_fif' field value.
  uint32_t CanFdFi::get_ID_MEM1_FIF_idm1_fif() const
  {
    const uint32_t raw_value = get_ID_MEM1_FIF_raw();

    return get_ID_MEM1_FIF_idm1_fif_from_raw(raw_value);
  }

  // Slice out the 'idm1_fif' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM1_FIF_idm1_fif_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM1_FIF::idm1_fif::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM1_FIF::idm1_fif::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM1_FIF(
    can_fd_fi::ID_MEM1_FIF::Value register_value
  ) const
  {
    const uint32_t idm1_fif_value = get_ID_MEM1_FIF_idm1_fif_to_raw(register_value.idm1_fif);
    const uint32_t raw_value = idm1_fif_value;

    set_ID_MEM1_FIF_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM1_FIF_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 13;
    m_registers[index] = register_value;
  }

  // Write the 'idm1_fif' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM1_FIF_idm1_fif(
    uint32_t field_value
  ) const
  {
    const size_t index = 13;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM1_FIF_idm1_fif_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm1_fif' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM1_FIF_idm1_fif_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 262143,
      "Got 'idm1_fif' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM1_FIF::idm1_fif::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM2_MATCH' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM2_MATCH::Value CanFdFi::get_ID_MEM2_MATCH() const
  {
    const uint32_t raw_value = get_ID_MEM2_MATCH_raw();

    const uint32_t idm2_match_value = get_ID_MEM2_MATCH_idm2_match_from_raw(raw_value);

    return {idm2_match_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM2_MATCH_raw() const
  {
    const size_t index = 14;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm2_match' field value.
  uint32_t CanFdFi::get_ID_MEM2_MATCH_idm2_match() const
  {
    const uint32_t raw_value = get_ID_MEM2_MATCH_raw();

    return get_ID_MEM2_MATCH_idm2_match_from_raw(raw_value);
  }

  // Slice out the 'idm2_match' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM2_MATCH_idm2_match_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM2_MATCH::idm2_match::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM2_MATCH::idm2_match::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM2_MATCH(
    can_fd_fi::ID_MEM2_MATCH::Value register_value
  ) const
  {
    const uint32_t idm2_match_value = get_ID_MEM2_MATCH_idm2_match_to_raw(register_value.idm2_match);
    const uint32_t raw_value = idm2_match_value;

    set_ID_MEM2_MATCH_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM2_MATCH_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 14;
    m_registers[index] = register_value;
  }

  // Write the 'idm2_match' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM2_MATCH_idm2_match(
    uint32_t field_value
  ) const
  {
    const size_t index = 14;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM2_MATCH_idm2_match_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm2_match' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM2_MATCH_idm2_match_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 536870911,
      "Got 'idm2_match' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM2_MATCH::idm2_match::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM2_MASK' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM2_MASK::Value CanFdFi::get_ID_MEM2_MASK() const
  {
    const uint32_t raw_value = get_ID_MEM2_MASK_raw();

    const uint32_t idm2_mask_value = get_ID_MEM2_MASK_idm2_mask_from_raw(raw_value);

    return {idm2_mask_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM2_MASK_raw() const
  {
    const size_t index = 15;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm2_mask' field value.
  uint32_t CanFdFi::get_ID_MEM2_MASK_idm2_mask() const
  {
    const uint32_t raw_value = get_ID_MEM2_MASK_raw();

    return get_ID_MEM2_MASK_idm2_mask_from_raw(raw_value);
  }

  // Slice out the 'idm2_mask' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM2_MASK_idm2_mask_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM2_MASK::idm2_mask::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM2_MASK::idm2_mask::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM2_MASK(
    can_fd_fi::ID_MEM2_MASK::Value register_value
  ) const
  {
    const uint32_t idm2_mask_value = get_ID_MEM2_MASK_idm2_mask_to_raw(register_value.idm2_mask);
    const uint32_t raw_value = idm2_mask_value;

    set_ID_MEM2_MASK_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM2_MASK_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 15;
    m_registers[index] = register_value;
  }

  // Write the 'idm2_mask' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM2_MASK_idm2_mask(
    uint32_t field_value
  ) const
  {
    const size_t index = 15;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM2_MASK_idm2_mask_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm2_mask' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM2_MASK_idm2_mask_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 536870911,
      "Got 'idm2_mask' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM2_MASK::idm2_mask::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM2_FIF' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM2_FIF::Value CanFdFi::get_ID_MEM2_FIF() const
  {
    const uint32_t raw_value = get_ID_MEM2_FIF_raw();

    const uint32_t idm2_fif_value = get_ID_MEM2_FIF_idm2_fif_from_raw(raw_value);

    return {idm2_fif_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM2_FIF_raw() const
  {
    const size_t index = 16;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm2_fif' field value.
  uint32_t CanFdFi::get_ID_MEM2_FIF_idm2_fif() const
  {
    const uint32_t raw_value = get_ID_MEM2_FIF_raw();

    return get_ID_MEM2_FIF_idm2_fif_from_raw(raw_value);
  }

  // Slice out the 'idm2_fif' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM2_FIF_idm2_fif_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM2_FIF::idm2_fif::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM2_FIF::idm2_fif::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM2_FIF(
    can_fd_fi::ID_MEM2_FIF::Value register_value
  ) const
  {
    const uint32_t idm2_fif_value = get_ID_MEM2_FIF_idm2_fif_to_raw(register_value.idm2_fif);
    const uint32_t raw_value = idm2_fif_value;

    set_ID_MEM2_FIF_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM2_FIF_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 16;
    m_registers[index] = register_value;
  }

  // Write the 'idm2_fif' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM2_FIF_idm2_fif(
    uint32_t field_value
  ) const
  {
    const size_t index = 16;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM2_FIF_idm2_fif_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm2_fif' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM2_FIF_idm2_fif_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 262143,
      "Got 'idm2_fif' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM2_FIF::idm2_fif::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM3_MATCH' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM3_MATCH::Value CanFdFi::get_ID_MEM3_MATCH() const
  {
    const uint32_t raw_value = get_ID_MEM3_MATCH_raw();

    const uint32_t idm3_match_value = get_ID_MEM3_MATCH_idm3_match_from_raw(raw_value);

    return {idm3_match_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM3_MATCH_raw() const
  {
    const size_t index = 17;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm3_match' field value.
  uint32_t CanFdFi::get_ID_MEM3_MATCH_idm3_match() const
  {
    const uint32_t raw_value = get_ID_MEM3_MATCH_raw();

    return get_ID_MEM3_MATCH_idm3_match_from_raw(raw_value);
  }

  // Slice out the 'idm3_match' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM3_MATCH_idm3_match_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM3_MATCH::idm3_match::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM3_MATCH::idm3_match::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM3_MATCH(
    can_fd_fi::ID_MEM3_MATCH::Value register_value
  ) const
  {
    const uint32_t idm3_match_value = get_ID_MEM3_MATCH_idm3_match_to_raw(register_value.idm3_match);
    const uint32_t raw_value = idm3_match_value;

    set_ID_MEM3_MATCH_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM3_MATCH_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 17;
    m_registers[index] = register_value;
  }

  // Write the 'idm3_match' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM3_MATCH_idm3_match(
    uint32_t field_value
  ) const
  {
    const size_t index = 17;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM3_MATCH_idm3_match_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm3_match' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM3_MATCH_idm3_match_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 536870911,
      "Got 'idm3_match' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM3_MATCH::idm3_match::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM3_MASK' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM3_MASK::Value CanFdFi::get_ID_MEM3_MASK() const
  {
    const uint32_t raw_value = get_ID_MEM3_MASK_raw();

    const uint32_t idm3_mask_value = get_ID_MEM3_MASK_idm3_mask_from_raw(raw_value);

    return {idm3_mask_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM3_MASK_raw() const
  {
    const size_t index = 18;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm3_mask' field value.
  uint32_t CanFdFi::get_ID_MEM3_MASK_idm3_mask() const
  {
    const uint32_t raw_value = get_ID_MEM3_MASK_raw();

    return get_ID_MEM3_MASK_idm3_mask_from_raw(raw_value);
  }

  // Slice out the 'idm3_mask' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM3_MASK_idm3_mask_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM3_MASK::idm3_mask::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM3_MASK::idm3_mask::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM3_MASK(
    can_fd_fi::ID_MEM3_MASK::Value register_value
  ) const
  {
    const uint32_t idm3_mask_value = get_ID_MEM3_MASK_idm3_mask_to_raw(register_value.idm3_mask);
    const uint32_t raw_value = idm3_mask_value;

    set_ID_MEM3_MASK_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM3_MASK_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 18;
    m_registers[index] = register_value;
  }

  // Write the 'idm3_mask' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM3_MASK_idm3_mask(
    uint32_t field_value
  ) const
  {
    const size_t index = 18;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM3_MASK_idm3_mask_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm3_mask' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM3_MASK_idm3_mask_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 536870911,
      "Got 'idm3_mask' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM3_MASK::idm3_mask::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM3_FIF' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM3_FIF::Value CanFdFi::get_ID_MEM3_FIF() const
  {
    const uint32_t raw_value = get_ID_MEM3_FIF_raw();

    const uint32_t idm3_fif_value = get_ID_MEM3_FIF_idm3_fif_from_raw(raw_value);

    return {idm3_fif_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM3_FIF_raw() const
  {
    const size_t index = 19;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm3_fif' field value.
  uint32_t CanFdFi::get_ID_MEM3_FIF_idm3_fif() const
  {
    const uint32_t raw_value = get_ID_MEM3_FIF_raw();

    return get_ID_MEM3_FIF_idm3_fif_from_raw(raw_value);
  }

  // Slice out the 'idm3_fif' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM3_FIF_idm3_fif_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM3_FIF::idm3_fif::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM3_FIF::idm3_fif::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM3_FIF(
    can_fd_fi::ID_MEM3_FIF::Value register_value
  ) const
  {
    const uint32_t idm3_fif_value = get_ID_MEM3_FIF_idm3_fif_to_raw(register_value.idm3_fif);
    const uint32_t raw_value = idm3_fif_value;

    set_ID_MEM3_FIF_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM3_FIF_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 19;
    m_registers[index] = register_value;
  }

  // Write the 'idm3_fif' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM3_FIF_idm3_fif(
    uint32_t field_value
  ) const
  {
    const size_t index = 19;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM3_FIF_idm3_fif_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm3_fif' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM3_FIF_idm3_fif_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 262143,
      "Got 'idm3_fif' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM3_FIF::idm3_fif::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM4_MATCH' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM4_MATCH::Value CanFdFi::get_ID_MEM4_MATCH() const
  {
    const uint32_t raw_value = get_ID_MEM4_MATCH_raw();

    const uint32_t idm4_match_value = get_ID_MEM4_MATCH_idm4_match_from_raw(raw_value);

    return {idm4_match_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM4_MATCH_raw() const
  {
    const size_t index = 20;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm4_match' field value.
  uint32_t CanFdFi::get_ID_MEM4_MATCH_idm4_match() const
  {
    const uint32_t raw_value = get_ID_MEM4_MATCH_raw();

    return get_ID_MEM4_MATCH_idm4_match_from_raw(raw_value);
  }

  // Slice out the 'idm4_match' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM4_MATCH_idm4_match_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM4_MATCH::idm4_match::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM4_MATCH::idm4_match::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM4_MATCH(
    can_fd_fi::ID_MEM4_MATCH::Value register_value
  ) const
  {
    const uint32_t idm4_match_value = get_ID_MEM4_MATCH_idm4_match_to_raw(register_value.idm4_match);
    const uint32_t raw_value = idm4_match_value;

    set_ID_MEM4_MATCH_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM4_MATCH_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 20;
    m_registers[index] = register_value;
  }

  // Write the 'idm4_match' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM4_MATCH_idm4_match(
    uint32_t field_value
  ) const
  {
    const size_t index = 20;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM4_MATCH_idm4_match_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm4_match' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM4_MATCH_idm4_match_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 536870911,
      "Got 'idm4_match' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM4_MATCH::idm4_match::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM4_MASK' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM4_MASK::Value CanFdFi::get_ID_MEM4_MASK() const
  {
    const uint32_t raw_value = get_ID_MEM4_MASK_raw();

    const uint32_t idm4_mask_value = get_ID_MEM4_MASK_idm4_mask_from_raw(raw_value);

    return {idm4_mask_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM4_MASK_raw() const
  {
    const size_t index = 21;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm4_mask' field value.
  uint32_t CanFdFi::get_ID_MEM4_MASK_idm4_mask() const
  {
    const uint32_t raw_value = get_ID_MEM4_MASK_raw();

    return get_ID_MEM4_MASK_idm4_mask_from_raw(raw_value);
  }

  // Slice out the 'idm4_mask' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM4_MASK_idm4_mask_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM4_MASK::idm4_mask::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM4_MASK::idm4_mask::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM4_MASK(
    can_fd_fi::ID_MEM4_MASK::Value register_value
  ) const
  {
    const uint32_t idm4_mask_value = get_ID_MEM4_MASK_idm4_mask_to_raw(register_value.idm4_mask);
    const uint32_t raw_value = idm4_mask_value;

    set_ID_MEM4_MASK_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM4_MASK_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 21;
    m_registers[index] = register_value;
  }

  // Write the 'idm4_mask' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM4_MASK_idm4_mask(
    uint32_t field_value
  ) const
  {
    const size_t index = 21;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM4_MASK_idm4_mask_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm4_mask' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM4_MASK_idm4_mask_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 536870911,
      "Got 'idm4_mask' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM4_MASK::idm4_mask::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM4_FIF' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM4_FIF::Value CanFdFi::get_ID_MEM4_FIF() const
  {
    const uint32_t raw_value = get_ID_MEM4_FIF_raw();

    const uint32_t idm4_fif_value = get_ID_MEM4_FIF_idm4_fif_from_raw(raw_value);

    return {idm4_fif_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM4_FIF_raw() const
  {
    const size_t index = 22;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm4_fif' field value.
  uint32_t CanFdFi::get_ID_MEM4_FIF_idm4_fif() const
  {
    const uint32_t raw_value = get_ID_MEM4_FIF_raw();

    return get_ID_MEM4_FIF_idm4_fif_from_raw(raw_value);
  }

  // Slice out the 'idm4_fif' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM4_FIF_idm4_fif_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM4_FIF::idm4_fif::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM4_FIF::idm4_fif::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM4_FIF(
    can_fd_fi::ID_MEM4_FIF::Value register_value
  ) const
  {
    const uint32_t idm4_fif_value = get_ID_MEM4_FIF_idm4_fif_to_raw(register_value.idm4_fif);
    const uint32_t raw_value = idm4_fif_value;

    set_ID_MEM4_FIF_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM4_FIF_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 22;
    m_registers[index] = register_value;
  }

  // Write the 'idm4_fif' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM4_FIF_idm4_fif(
    uint32_t field_value
  ) const
  {
    const size_t index = 22;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM4_FIF_idm4_fif_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm4_fif' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM4_FIF_idm4_fif_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 262143,
      "Got 'idm4_fif' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM4_FIF::idm4_fif::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM5_MATCH' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM5_MATCH::Value CanFdFi::get_ID_MEM5_MATCH() const
  {
    const uint32_t raw_value = get_ID_MEM5_MATCH_raw();

    const uint32_t idm5_match_value = get_ID_MEM5_MATCH_idm5_match_from_raw(raw_value);

    return {idm5_match_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM5_MATCH_raw() const
  {
    const size_t index = 23;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm5_match' field value.
  uint32_t CanFdFi::get_ID_MEM5_MATCH_idm5_match() const
  {
    const uint32_t raw_value = get_ID_MEM5_MATCH_raw();

    return get_ID_MEM5_MATCH_idm5_match_from_raw(raw_value);
  }

  // Slice out the 'idm5_match' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM5_MATCH_idm5_match_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM5_MATCH::idm5_match::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM5_MATCH::idm5_match::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM5_MATCH(
    can_fd_fi::ID_MEM5_MATCH::Value register_value
  ) const
  {
    const uint32_t idm5_match_value = get_ID_MEM5_MATCH_idm5_match_to_raw(register_value.idm5_match);
    const uint32_t raw_value = idm5_match_value;

    set_ID_MEM5_MATCH_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM5_MATCH_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 23;
    m_registers[index] = register_value;
  }

  // Write the 'idm5_match' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM5_MATCH_idm5_match(
    uint32_t field_value
  ) const
  {
    const size_t index = 23;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM5_MATCH_idm5_match_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm5_match' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM5_MATCH_idm5_match_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 536870911,
      "Got 'idm5_match' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM5_MATCH::idm5_match::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM5_MASK' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM5_MASK::Value CanFdFi::get_ID_MEM5_MASK() const
  {
    const uint32_t raw_value = get_ID_MEM5_MASK_raw();

    const uint32_t idm5_mask_value = get_ID_MEM5_MASK_idm5_mask_from_raw(raw_value);

    return {idm5_mask_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM5_MASK_raw() const
  {
    const size_t index = 24;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm5_mask' field value.
  uint32_t CanFdFi::get_ID_MEM5_MASK_idm5_mask() const
  {
    const uint32_t raw_value = get_ID_MEM5_MASK_raw();

    return get_ID_MEM5_MASK_idm5_mask_from_raw(raw_value);
  }

  // Slice out the 'idm5_mask' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM5_MASK_idm5_mask_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM5_MASK::idm5_mask::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM5_MASK::idm5_mask::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM5_MASK(
    can_fd_fi::ID_MEM5_MASK::Value register_value
  ) const
  {
    const uint32_t idm5_mask_value = get_ID_MEM5_MASK_idm5_mask_to_raw(register_value.idm5_mask);
    const uint32_t raw_value = idm5_mask_value;

    set_ID_MEM5_MASK_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM5_MASK_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 24;
    m_registers[index] = register_value;
  }

  // Write the 'idm5_mask' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM5_MASK_idm5_mask(
    uint32_t field_value
  ) const
  {
    const size_t index = 24;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM5_MASK_idm5_mask_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm5_mask' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM5_MASK_idm5_mask_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 536870911,
      "Got 'idm5_mask' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM5_MASK::idm5_mask::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM5_FIF' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM5_FIF::Value CanFdFi::get_ID_MEM5_FIF() const
  {
    const uint32_t raw_value = get_ID_MEM5_FIF_raw();

    const uint32_t idm5_fif_value = get_ID_MEM5_FIF_idm5_fif_from_raw(raw_value);

    return {idm5_fif_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM5_FIF_raw() const
  {
    const size_t index = 25;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm5_fif' field value.
  uint32_t CanFdFi::get_ID_MEM5_FIF_idm5_fif() const
  {
    const uint32_t raw_value = get_ID_MEM5_FIF_raw();

    return get_ID_MEM5_FIF_idm5_fif_from_raw(raw_value);
  }

  // Slice out the 'idm5_fif' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM5_FIF_idm5_fif_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM5_FIF::idm5_fif::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM5_FIF::idm5_fif::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM5_FIF(
    can_fd_fi::ID_MEM5_FIF::Value register_value
  ) const
  {
    const uint32_t idm5_fif_value = get_ID_MEM5_FIF_idm5_fif_to_raw(register_value.idm5_fif);
    const uint32_t raw_value = idm5_fif_value;

    set_ID_MEM5_FIF_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM5_FIF_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 25;
    m_registers[index] = register_value;
  }

  // Write the 'idm5_fif' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM5_FIF_idm5_fif(
    uint32_t field_value
  ) const
  {
    const size_t index = 25;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM5_FIF_idm5_fif_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm5_fif' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM5_FIF_idm5_fif_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 262143,
      "Got 'idm5_fif' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM5_FIF::idm5_fif::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM6_MATCH' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM6_MATCH::Value CanFdFi::get_ID_MEM6_MATCH() const
  {
    const uint32_t raw_value = get_ID_MEM6_MATCH_raw();

    const uint32_t idm6_match_value = get_ID_MEM6_MATCH_idm6_match_from_raw(raw_value);

    return {idm6_match_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM6_MATCH_raw() const
  {
    const size_t index = 26;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm6_match' field value.
  uint32_t CanFdFi::get_ID_MEM6_MATCH_idm6_match() const
  {
    const uint32_t raw_value = get_ID_MEM6_MATCH_raw();

    return get_ID_MEM6_MATCH_idm6_match_from_raw(raw_value);
  }

  // Slice out the 'idm6_match' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM6_MATCH_idm6_match_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM6_MATCH::idm6_match::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM6_MATCH::idm6_match::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM6_MATCH(
    can_fd_fi::ID_MEM6_MATCH::Value register_value
  ) const
  {
    const uint32_t idm6_match_value = get_ID_MEM6_MATCH_idm6_match_to_raw(register_value.idm6_match);
    const uint32_t raw_value = idm6_match_value;

    set_ID_MEM6_MATCH_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM6_MATCH_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 26;
    m_registers[index] = register_value;
  }

  // Write the 'idm6_match' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM6_MATCH_idm6_match(
    uint32_t field_value
  ) const
  {
    const size_t index = 26;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM6_MATCH_idm6_match_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm6_match' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM6_MATCH_idm6_match_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 536870911,
      "Got 'idm6_match' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM6_MATCH::idm6_match::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM6_MASK' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM6_MASK::Value CanFdFi::get_ID_MEM6_MASK() const
  {
    const uint32_t raw_value = get_ID_MEM6_MASK_raw();

    const uint32_t idm6_mask_value = get_ID_MEM6_MASK_idm6_mask_from_raw(raw_value);

    return {idm6_mask_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM6_MASK_raw() const
  {
    const size_t index = 27;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm6_mask' field value.
  uint32_t CanFdFi::get_ID_MEM6_MASK_idm6_mask() const
  {
    const uint32_t raw_value = get_ID_MEM6_MASK_raw();

    return get_ID_MEM6_MASK_idm6_mask_from_raw(raw_value);
  }

  // Slice out the 'idm6_mask' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM6_MASK_idm6_mask_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM6_MASK::idm6_mask::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM6_MASK::idm6_mask::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM6_MASK(
    can_fd_fi::ID_MEM6_MASK::Value register_value
  ) const
  {
    const uint32_t idm6_mask_value = get_ID_MEM6_MASK_idm6_mask_to_raw(register_value.idm6_mask);
    const uint32_t raw_value = idm6_mask_value;

    set_ID_MEM6_MASK_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM6_MASK_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 27;
    m_registers[index] = register_value;
  }

  // Write the 'idm6_mask' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM6_MASK_idm6_mask(
    uint32_t field_value
  ) const
  {
    const size_t index = 27;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM6_MASK_idm6_mask_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm6_mask' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM6_MASK_idm6_mask_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 536870911,
      "Got 'idm6_mask' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM6_MASK::idm6_mask::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM6_FIF' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM6_FIF::Value CanFdFi::get_ID_MEM6_FIF() const
  {
    const uint32_t raw_value = get_ID_MEM6_FIF_raw();

    const uint32_t idm6_fif_value = get_ID_MEM6_FIF_idm6_fif_from_raw(raw_value);

    return {idm6_fif_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM6_FIF_raw() const
  {
    const size_t index = 28;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm6_fif' field value.
  uint32_t CanFdFi::get_ID_MEM6_FIF_idm6_fif() const
  {
    const uint32_t raw_value = get_ID_MEM6_FIF_raw();

    return get_ID_MEM6_FIF_idm6_fif_from_raw(raw_value);
  }

  // Slice out the 'idm6_fif' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM6_FIF_idm6_fif_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM6_FIF::idm6_fif::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM6_FIF::idm6_fif::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM6_FIF(
    can_fd_fi::ID_MEM6_FIF::Value register_value
  ) const
  {
    const uint32_t idm6_fif_value = get_ID_MEM6_FIF_idm6_fif_to_raw(register_value.idm6_fif);
    const uint32_t raw_value = idm6_fif_value;

    set_ID_MEM6_FIF_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM6_FIF_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 28;
    m_registers[index] = register_value;
  }

  // Write the 'idm6_fif' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM6_FIF_idm6_fif(
    uint32_t field_value
  ) const
  {
    const size_t index = 28;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM6_FIF_idm6_fif_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm6_fif' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM6_FIF_idm6_fif_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 262143,
      "Got 'idm6_fif' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM6_FIF::idm6_fif::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM7_MATCH' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM7_MATCH::Value CanFdFi::get_ID_MEM7_MATCH() const
  {
    const uint32_t raw_value = get_ID_MEM7_MATCH_raw();

    const uint32_t idm7_match_value = get_ID_MEM7_MATCH_idm7_match_from_raw(raw_value);

    return {idm7_match_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM7_MATCH_raw() const
  {
    const size_t index = 29;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm7_match' field value.
  uint32_t CanFdFi::get_ID_MEM7_MATCH_idm7_match() const
  {
    const uint32_t raw_value = get_ID_MEM7_MATCH_raw();

    return get_ID_MEM7_MATCH_idm7_match_from_raw(raw_value);
  }

  // Slice out the 'idm7_match' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM7_MATCH_idm7_match_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM7_MATCH::idm7_match::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM7_MATCH::idm7_match::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM7_MATCH(
    can_fd_fi::ID_MEM7_MATCH::Value register_value
  ) const
  {
    const uint32_t idm7_match_value = get_ID_MEM7_MATCH_idm7_match_to_raw(register_value.idm7_match);
    const uint32_t raw_value = idm7_match_value;

    set_ID_MEM7_MATCH_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM7_MATCH_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 29;
    m_registers[index] = register_value;
  }

  // Write the 'idm7_match' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM7_MATCH_idm7_match(
    uint32_t field_value
  ) const
  {
    const size_t index = 29;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM7_MATCH_idm7_match_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm7_match' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM7_MATCH_idm7_match_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 536870911,
      "Got 'idm7_match' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM7_MATCH::idm7_match::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM7_MASK' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM7_MASK::Value CanFdFi::get_ID_MEM7_MASK() const
  {
    const uint32_t raw_value = get_ID_MEM7_MASK_raw();

    const uint32_t idm7_mask_value = get_ID_MEM7_MASK_idm7_mask_from_raw(raw_value);

    return {idm7_mask_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM7_MASK_raw() const
  {
    const size_t index = 30;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm7_mask' field value.
  uint32_t CanFdFi::get_ID_MEM7_MASK_idm7_mask() const
  {
    const uint32_t raw_value = get_ID_MEM7_MASK_raw();

    return get_ID_MEM7_MASK_idm7_mask_from_raw(raw_value);
  }

  // Slice out the 'idm7_mask' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM7_MASK_idm7_mask_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM7_MASK::idm7_mask::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM7_MASK::idm7_mask::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM7_MASK(
    can_fd_fi::ID_MEM7_MASK::Value register_value
  ) const
  {
    const uint32_t idm7_mask_value = get_ID_MEM7_MASK_idm7_mask_to_raw(register_value.idm7_mask);
    const uint32_t raw_value = idm7_mask_value;

    set_ID_MEM7_MASK_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM7_MASK_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 30;
    m_registers[index] = register_value;
  }

  // Write the 'idm7_mask' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM7_MASK_idm7_mask(
    uint32_t field_value
  ) const
  {
    const size_t index = 30;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM7_MASK_idm7_mask_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm7_mask' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM7_MASK_idm7_mask_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 536870911,
      "Got 'idm7_mask' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM7_MASK::idm7_mask::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'ID_MEM7_FIF' register.
  // Mode 'Read, Write'.
  // ---------------------------------------------------------------------------
  // Read the whole register value over the register bus.
  can_fd_fi::ID_MEM7_FIF::Value CanFdFi::get_ID_MEM7_FIF() const
  {
    const uint32_t raw_value = get_ID_MEM7_FIF_raw();

    const uint32_t idm7_fif_value = get_ID_MEM7_FIF_idm7_fif_from_raw(raw_value);

    return {idm7_fif_value};
  }

  // Read the whole register value over the register bus.
  // This method returns the raw register value without any type conversion.
  uint32_t CanFdFi::get_ID_MEM7_FIF_raw() const
  {
    const size_t index = 31;
    const uint32_t raw_value = m_registers[index];

    return raw_value;
  }

  // Read the register and slice out the 'idm7_fif' field value.
  uint32_t CanFdFi::get_ID_MEM7_FIF_idm7_fif() const
  {
    const uint32_t raw_value = get_ID_MEM7_FIF_raw();

    return get_ID_MEM7_FIF_idm7_fif_from_raw(raw_value);
  }

  // Slice out the 'idm7_fif' field value from a given raw register value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM7_FIF_idm7_fif_from_raw(
    uint32_t register_value
  ) const
  {
    const uint32_t result_masked = register_value & can_fd_fi::ID_MEM7_FIF::idm7_fif::mask_shifted;
    const uint32_t result_shifted = result_masked >> can_fd_fi::ID_MEM7_FIF::idm7_fif::shift;

    // No casting needed.
    const uint32_t field_value = result_shifted;

    return field_value;
  }

  // Write the whole register value over the register bus.
  void CanFdFi::set_ID_MEM7_FIF(
    can_fd_fi::ID_MEM7_FIF::Value register_value
  ) const
  {
    const uint32_t idm7_fif_value = get_ID_MEM7_FIF_idm7_fif_to_raw(register_value.idm7_fif);
    const uint32_t raw_value = idm7_fif_value;

    set_ID_MEM7_FIF_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_ID_MEM7_FIF_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 31;
    m_registers[index] = register_value;
  }

  // Write the 'idm7_fif' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_ID_MEM7_FIF_idm7_fif(
    uint32_t field_value
  ) const
  {
    const size_t index = 31;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_ID_MEM7_FIF_idm7_fif_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'idm7_fif' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_ID_MEM7_FIF_idm7_fif_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 262143,
      "Got 'idm7_fif' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::ID_MEM7_FIF::idm7_fif::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'MU_CFG_BASE' register.
  // Mode 'Write'.
  // ---------------------------------------------------------------------------
  // Write the whole register value over the register bus.
  void CanFdFi::set_MU_CFG_BASE(
    can_fd_fi::MU_CFG_BASE::Value register_value
  ) const
  {
    const uint32_t ram_data_valid_value = get_MU_CFG_BASE_ram_data_valid_to_raw(register_value.ram_data_valid);
    const uint32_t raw_value = ram_data_valid_value;

    set_MU_CFG_BASE_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_MU_CFG_BASE_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 32;
    m_registers[index] = register_value;
  }

  // Write the 'ram_data_valid' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_MU_CFG_BASE_ram_data_valid(
    bool field_value
  ) const
  {
    const size_t index = 32;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_MU_CFG_BASE_ram_data_valid_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'ram_data_valid' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_MU_CFG_BASE_ram_data_valid_to_raw(bool field_value) const
  {
    const uint32_t field_value_casted = static_cast<uint32_t>(field_value);
    const uint32_t field_value_shifted = field_value_casted << can_fd_fi::MU_CFG_BASE::ram_data_valid::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'MU_CFG_ADDR' register.
  // Mode 'Write'.
  // ---------------------------------------------------------------------------
  // Write the whole register value over the register bus.
  void CanFdFi::set_MU_CFG_ADDR(
    can_fd_fi::MU_CFG_ADDR::Value register_value
  ) const
  {
    const uint32_t ram_data_address_value = get_MU_CFG_ADDR_ram_data_address_to_raw(register_value.ram_data_address);
    const uint32_t raw_value = ram_data_address_value;

    set_MU_CFG_ADDR_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_MU_CFG_ADDR_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 33;
    m_registers[index] = register_value;
  }

  // Write the 'ram_data_address' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_MU_CFG_ADDR_ram_data_address(
    uint32_t field_value
  ) const
  {
    const size_t index = 33;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_MU_CFG_ADDR_ram_data_address_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'ram_data_address' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_MU_CFG_ADDR_ram_data_address_to_raw(uint32_t field_value) const
  {
    _SETTER_ASSERT_TRUE(
      field_value <= 32767,
      "Got 'ram_data_address' value out of range: " << field_value
    );

    const uint32_t field_value_shifted = field_value << can_fd_fi::MU_CFG_ADDR::ram_data_address::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // Methods for the 'MU_RAM_DATA' register.
  // Mode 'Write'.
  // ---------------------------------------------------------------------------
  // Write the whole register value over the register bus.
  void CanFdFi::set_MU_RAM_DATA(
    can_fd_fi::MU_RAM_DATA::Value register_value
  ) const
  {
    const uint32_t ram_data_payload_value = get_MU_RAM_DATA_ram_data_payload_to_raw(register_value.ram_data_payload);
    const uint32_t raw_value = ram_data_payload_value;

    set_MU_RAM_DATA_raw(raw_value);
  }

  // Write the whole register value over the register bus.
  // This method takes a raw register value and does not perform any type conversion.
  void CanFdFi::set_MU_RAM_DATA_raw(
    uint32_t register_value
  ) const
  {
    const size_t index = 34;
    m_registers[index] = register_value;
  }

  // Write the 'ram_data_payload' field value.
  // Will write the register with all other fields set as default.
  void CanFdFi::set_MU_RAM_DATA_ram_data_payload(
    uint32_t field_value
  ) const
  {
    const size_t index = 34;

    const uint32_t base_value = 0;

    const uint32_t field_value_raw = get_MU_RAM_DATA_ram_data_payload_to_raw(field_value);
    const uint32_t register_value = base_value | field_value_raw;

    m_registers[index] = register_value;
  }

  // Get the raw representation of a given 'ram_data_payload' field value.
  // Performs no operation on the register bus.
  uint32_t CanFdFi::get_MU_RAM_DATA_ram_data_payload_to_raw(uint32_t field_value) const
  {
    const uint32_t field_value_shifted = field_value << can_fd_fi::MU_RAM_DATA::ram_data_payload::shift;

    return field_value_shifted;
  }
  // ---------------------------------------------------------------------------

} // namespace fpga_regs
